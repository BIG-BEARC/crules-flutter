# 评审-plan：孪生同文块生成化（F3）

> 状态：**已闭环**——所评方案 P1 落地（1.0.12）、P2/P3 落账（1.0.15；状态戳 2026-09-16 补——D2 纪律缺口由 flutter-09 清点发现）；注：文内 tp_sect/tp_tail/tp_gate 系 P3 已删的旧 byte 互锁断言函数，仅存史证勿向 test-self.sh 索引 · 评审日期：2026-09-15
> **评审纪律声明**：本档由独立上下文 subagent（plan-reviewer，只读：Read/Glob/Grep）产出，**只评审、不修改**被评审方案，不调用任何修改工具。结论**不经主控复核过滤**，由主控原样落盘（只机械转写返回值）。评审日期：2026-09-15。评审对象：[方案-2026-09-15-孪生同文块生成化.md](方案-2026-09-15-孪生同文块生成化.md) v1。
> **评审方法**：只读方案成品（未喂讨论过程与产出者倾向）；独立核对 `scripts/test-self.sh`（实跑其 `tp_sect`/`tp_tail`/`tp_gate` 抽取函数取 sha）、双模板、help/README、`release.sh`/`install.sh` 分发链。

---

## 核验结论（前提成立性）

- **四块确实逐字同文**：sha 实测 app↔plugin 一致（`f2c0eb44…` / `42f7388c…` / `a162e767…`；help.md:26 与 README.md:66 同 hash `35ebe148…`）——方案前提 1 对第 1-4 块成立
- 27 断言静态计数确认；约 10 条为文档孪生守护（「约 1/3」成立）
- 两模板 651 行中 **207 行逐字相同**，被断言守护的仅 3 段
- 未运行 `test-self.sh` 本体（其写 /tmp）；未修改任何文件

## 发现清单

| # | 发现 | 严重程度 | 原因 / 证据 | 建议（只建议，不改） |
|---|------|---------|------------|-------------------|
| 1 | 第 1 块（档位预设）**不是「不可自改」块**，方案前提自相矛盾 | 🔴 | 该块标题自带消费方编辑指令 `### 档位预设（init 选一，删未选两段；无块 = 默认标准）`（`app/CLAUDE.md:329` / `plugin/CLAUDE.md:308`），且 `commands/init.md:44` 明令「选定后删掉模板 §十二 未选的两段预设块」。方案 §1「不做什么」第 2 条与文末「与『复制后必填』的关系」两处断言「生成的正是**不可自改**的同文块…不冲突」——与事实相反。二次伤害：D2 的围栏原文「请勿手改」会随 `scripts/install.sh` 的逐字 `cp`（`install.sh:46`）落进**每个消费方项目**，而消费方根本拿不到 `canonical/`，就地提示与 init 的「删未选两段」直接冲突 | 二选一：把第 1 块移出试点（只做 3 块），或把围栏文案限定为「**仓内**勿手改（本块由 canonical/ 生成）」并在 §1 显式写明消费方仍可删减 |
| 2 | 幂等断言可被「静默空转」假绿满足，方案「任何漂移都红」的核心论据不成立 | 🔴 | §4 判据为 `render && git diff --exit-code`。若 render 因围栏解析失败 / 0 处匹配而**什么都没做却 exit 0**，diff 为空 → CI 绿，四块此后可任意漂移。本仓已有同款教训的标准写法：`scripts/test-self.sh:83-84` 的 `ran` 守卫（注释原文「防『没跑起来却空过』的假绿」）。P1/P2 靠 D4 保留的旧断言兜住，**P3 删旧断言后即真空** | 要求 render 对「每个声明的 id 在目标文件中恰好注入 1 处」做计数断言，缺失即非零退出；并把「P3 删旧断言」的前置条件写成「计数守卫已就位」 |
| 3 | 第 3 块提取边界描述与真实 awk 不符 | 🟡 | §1 写「Gate 例外段（`^例外一律落` 起至空行）」，但 `tp_gate`（`test-self.sh:157`）的终止条件是**第二个**空行，实测切片含「正文 + 空行 + `---` 分隔线」（app:124-126 / plugin:117-119） | 按 awk 实测改写 §1 边界描述；canonical 的 `gate-exception.md` 是否含尾随空行与 `---` 必须写死，否则 §6 用例 2 的 hash 比对会出「非内容差异」的假失败 |
| 4 | 第 4 块被称作「段」，但现有断言只钉**单行** | 🟡 | §1 称 block 4 为「`收尾档按任务规模三档判定` **所在段**」，而 `test-self.sh:181-182` 用 `grep -m1` 只取一行；§3 却称四块边界「取自 test-self 现有断言定义」。该段无换行折行、单行即整段（help.md:26 / README.md:66），故不致命，但「段」的起止无既有断言背书，§6 用例 2 缺可比基线 | 承认第 4 块边界由本方案**新定义**（非沿用断言），并把该断言的 1 行口径与 canonical 文件范围对齐写明 |
| 5 | 「4 条旧断言 → 净减 3」的账目对不上 | 🟡 | 可被本方案替代的 **byte 级**互锁只有 2 条：`test-self.sh:169`（twin 档位块/收尾行/台账段，1 个 PASS）与 `:184`（help↔README）。`:151` 的档位四方同源闸是**存在性 grep**（19 串），四块内容不变则它既不会也不能被删。故 P3 实为「删 2 加 1 = 净 −1」；疑似把 tp_sect/tp_tail/tp_gate 三个**块**误记为三条断言。该数字是 §5 与文末「治理从简」的论据之一 | 改口径为「净 −1」并重述治理论据；若坚持「净减 3」，须指明第 3 条是什么 |
| 6 | 「canonical 不分发」不成立——它必然随 plugin cache 走 | 🟡 | D1 标 ✅ 亲验并断言「源不随分发面走（install.sh 不复制该目录）」。分发有两条链路：① `install.sh` 确为固定清单，不复制该目录（`install.sh:84-85`，已核）；② **plugin cache** —— `scripts/release.sh:8` 自述「cache 是**全仓库快照**（含 README/docs/scripts）」。canonical/ 若要被 CI 使用就必须进 git，进 git 即进 cache | 改为「不落**消费方项目**」，并说明 cache 会带源副本（惰性文件，无功能影响） |
| 7 | `bash scripts/render-blocks.py` 解释器自相矛盾 | 🟡 | §4 第 63 行验收命令用 `bash` 跑 `.py`；§4 第 48 行文件名是 `.py`，§9 Q1 建议 python。按字面执行即语法错误，验收判据 #1 无法通过 | 统一为 `python3 scripts/render-blocks.py` |
| 8 | 生成区域与断言区域的**包含关系**未定义（标题行在不在块内） | 🟡 | §4 示例围栏把 `### 档位预设（…无块 = 默认标准）` **包在块内**，而 `tp_sect`（`test-self.sh:155`）刻意 `next` **跳过**标题行。实测 ga 19 串中唯一落在三块之外的正是 `默认标准`，位置就在这行标题（app:329 / plugin:308）——标题归属直接决定首轮 render 是否与仓内现状逐字一致 | §4 显式规定标题行归属，并注明 canonical 文件是否含标题 |
| 9 | 自测将**原地改写被跟踪文件**，破坏现有只写 /tmp 的隔离 | 🟡 | 现 test-self 全部产物落 `/tmp`（`test-self.sh:13`）。若按 §4 写法在 test-self 内执行 render，会在维护者未提交的工作区重写四个被跟踪文件（`app/CLAUDE.md`、`plugin/CLAUDE.md`、`commands/help.md`、`README.md`）；render 有 bug 时直接损坏工作区，与既有「篡改自测禁 git 回滚」纪律相冲 | 断言改为在 /tmp 副本上渲染后与仓内比对；或限定仅 CI 执行、本地需干净树 |
| 10 | 「不做全量」结论正确，但论证不完整 | 🟢 | 实测两模板 651 行中 **207 行逐字相同**，被现有断言守护的只有 3 段；其余约 200 行是*偶然*同文的散落散文——拉进 byte 互锁即禁止合法分化，**这才是**「全量是幻觉」的真实依据。§1 只说了「生成本就无法表达」 | 把该实测数字补进 §1，使「试点范围 ≈ 理性全量范围」从断言变为证据 |
| 11 | 「指针化」更优解评估（重点项）——**结论：不适用，仅第 4 块可行且不省** | 🟢 | 先例 `twin:mem-files`/`twin:mem-trigger`（`memory/README.md:14`、`memory/MAINTENANCE.md:5,13`、`进阶/记忆库体系.md:18,48`）成立的前提是**两份副本与指针目标同处一份交付物**。第 1-3 块的 app/plugin 模板是**互斥交付**（install.sh 按 `--app`/`--plugin` 二选一，消费方只拿到一份，另一份不在场）→ 指针必悬空，结构性不适用。第 4 块指针化可行但**不省**：四方同源闸（`test-self.sh:147-149`）要求 README.md **字面**含 8 个 gc 串，指针化会一并废掉该断言，且 README 是对外门面 | 「生成化」对第 1-3 块成立；建议把这段「为何不用指针」写进 §2 决策留痕（现为空白） |
| 12 | 范围未夹带，且未漏掉 `init.md` | 🟢 | §4 只列 4 源 + 1 脚本，P2/P3 为观测与收尾，无夹带。`commands/init.md:44` 的档位文字是**改写非逐字同文**（措辞与 help/README 不同，已实测），正确地未被纳入，非遗漏 | 无需动作 |
| 13 | 围栏行数成本记错 | 🟢 | §5 / Q3 称常驻增「4 行」；实为第 1/2/3 块 × 开闭围栏 = **每模板 6 行**（help/README 另各 +2 行，且不入常驻面预算闸）。预算余量充足，非阻塞：app 15833/18000、plugin 14532/16500，单行约 25-28 字符 | 改数字即可 |
| 14 | 证据行号失准 | 🟢 | §3 引「`test-self.sh:140-156`」「`:166-174`」；实际 tp_* 定义与循环在 `:153-169`、help↔README 在 `:179-187`（四方同源闸在 `:132-151`）。文档自称 ✅ 亲验却行号偏移 | 校正行号 |

**补充核验**（未列入表）：围栏注入对现有断言的破坏面经逐条核对为**无**——`^- ` 条数闸（`tt_cnt`）、节序号集（`tt_sec`）、四方同源 grep 均不匹配 `<!--` 行；且因围栏注入对 app/plugin 是**对称**变换，D4 保留的 `tp_*` 逐字断言仍成立（逻辑推导非实跑，标**待证**）。常驻面字数预算闸有余量（见 #13）。

## 整体结论

**有 🔴 须消解**——第 1 条（档位预设块非「不可自改」与前提冲突）、第 2 条（幂等断言可被静默空转假绿）。两条均低成本可修：前者调整第 1 块范围或围栏文案，后者补「注入计数守卫」。其余 🟡 为口径与边界定义修正，不阻塞方向。方案技术方向（对**互斥交付**的第 1-3 块做生成化）本身成立，「指针化」替代解经先例查证不适用。

---

# 第二轮（diff 级复审 · v2 消解核验）

> **评审纪律声明**：同首轮——独立上下文 subagent（plan-reviewer，只读），只评审不修改，结论不过滤、主控原样落盘。复审日期：2026-09-15。对象：方案 v2。
> **方法**：只核 R1-R14 消解情况（diff 级，不重跑全量）；实读 `commands/init.md:44`、`test-self.sh`（实跑 `tp_sect`/`tp_gate`）、`install.sh`、`release.sh:8`、`ci.yml`、双模板 §十二/§三。

## 消解核验汇总

| 类别 | 结果 |
|---|---|
| R1 🔴 | **消解（骨架到位）**——✅ 复审核实：v2 围栏说明的「消费方按 init 引导删减未选段」分句**显式让位于 `init.md:44`**，与「删掉未选两段」同向不互斥，原「落进消费方冲突」的二次伤害消除。**但消解机制自身带来新缺口 → R17** |
| R2 🔴 | **部分消解**——原情形（0 匹配空转）已堵（计数 0 ≠ 1 → 非零退出）；**仍有两处假绿 → R15** |
| R3/R4/R5/R6/R7/R8/R10/R11/R12/R13/R14 | **消解**（其中 R3/R5/R14 经实跑/读码逐字核实）；R12 无需动作（复核实测 `init.md:44` 措辞确非逐字同文，正确未纳入） |
| R9 🟡 | **部分消解**——§6.2 已改 /tmp 副本，**但 §4 CI 断言落点未写明 → R16** |

**范围溢出**：**无**——v2 全部增量可回溯 R1-R14，未新增第 5 源或第 2 脚本。

## 新发现（消解自身引入）

| # | 发现 | 严重程度 | 证据 | 建议 |
|---|------|---------|------|------|
| R15 | 计数守卫缺「内容同步」半句，残留假绿 | 🟡 | §4 伪码仅落「围栏恰好 1 对」；D6 prose 的「渲染结果与 canonical 同步」未成步骤。仍假绿两情形：**(a) 围栏在、内容不同步**（canonical 改了而 render 未改写块内容，计数仍 1 → exit 0 → 绿）；**(b) EXPECTED 漏登记**（手工清单，新增第 5 块无守卫） | 判据加「围栏区间字节 == canonical」；EXPECTED 由手工清单改「扫描 `<!-- gen:` 标记，与 EXPECTED 键集合相等」 |
| R16 | §4 CI 断言落点未定，与 R9 的 /tmp 隔离相冲 | 🟡 | §4 断言在**仓内**跑 `git diff`；§6.2/§8 却要求 /tmp 副本。`ci.yml:30-32` 显示 CI 唯一入口是 `test-self.sh`（维护者本地也跑）→ 断言落此则本地运行即重写被跟踪文件 | 明写执行环境：限定 CI-only，或统一改 /tmp 副本比对 |
| R17 | **说明行落在围栏内，与「整块替换」语义冲突——R1 消解机制自毁** | 🟡 | §4 围栏示例把说明注释放在围栏**内**；而「以围栏内整块替换为 canonical 内容」→ **首轮 render 即抹除说明行**，D2 受众区分文案不复存在 | 说明行纳入 canonical（并同步目录说明），或规定 render 保留 `<!--` 说明行只替换正文 |
| R18 | 「6 行」算式仍不闭合 | 🟢 | 3 块 ×（开+说明+闭）= 9 行；仅第 1 块带说明则 7 行；均 ≠ 6 | 按实际落法重算分列 |
| R19 | §6.2 用例 5 目标文件写错 | 🟢 | 写「删 canonical/gear-note.md 的 1 对围栏」，但 canonical 为裸内容源、无围栏可删 | 改为删 `commands/help.md`（或 README.md）的围栏 |

## 整体结论

**仍有 3 条须再消解**（R15/R16/R17；R18/R19 同批顺手改）。R1 与 R2 两条 🔴 的**主干均已到位**（R1 与 `init.md:44` 核实不冲突；R2 原空转路径已堵），R3-R14 均已按实测校正，无范围溢出。**本轮实证了「消解 ≠ 闭环」**——v2 的修订动作本身引入了 5 条新缺口，其中 R17 是「修 R1 的动作反噬 R1 的成果」。
