# 实施计划：0.6.0 批（三方案并批落地 · 评审修订版 v2）

> 状态：已确认（方案裁决 + 计划评审一轮，评审意见 10+3 项全部吸收）· 依据：[方案-适配三选一与字体策略](方案-2026-09-08-适配三选一与字体策略.md) + [方案-IoT外设知识抽层](方案-2026-09-08-IoT外设知识抽层.md) + [吸收方案-订单折算复盘](吸收方案-2026-09-05-订单折算复盘.md)
> 版本口径：**0.6.0（minor）**——v1 曾判 0.5.3 patch，评审裁定：模板新增必选交互环节（适配三选一 + 字体策略）、改接入流程，按 0.5.0 先例（skill 拆分给 minor）升 minor。

## 评审裁决记录（v1 → v2 变更）

| # | 评审项 | 裁决 |
|---|---|---|
| 阻1 | plugin 模板落点结构不匹配 | 适配三选一/字体策略**仅落 app 模板**，以 `###` 子节入 §七/8.1 内（不新增 `##` 节——孪生守护断言节序号集，`###` 不炸）；plugin §八 +1 行指针「涉 UI 的 plugin 参考 app 模板 §七适配三选一」；文件头跟随双模板都落（语义各自适配） |
| 阻2 | R4 内容丢失 + 吸收 Q1 未裁决 | 吸收方案 Q1 裁决 = checklist 条件条；B5 落 R4 全文（见批 B 表内） |
| 建1 | error.md 重复实为 1 条 | B6 与验收改「1 条归并」（error.md:26 一次性消费 ↔ backend 铁律第 4 条） |
| 建2 | 断点档数两源打架 | 统一**三档**（Compact<600 / Medium<840 / Expanded≥840），桌面注「可扩 large/extra-large」 |
| 建3 | 版本口径 | 0.5.3 → **0.6.0** |
| 建4 | 孪生守护描述错位 | 更正为「节序号集 + §一/三/四/五/六 条数」；C4 人工跑 test-self 纳入验收 |
| 优1-4 | A1 归节 / screenutil=0 / backend:45-53 / fork-coverage 节题 | 全部采纳（见各任务行） |

## 任务清单（批 A → B → C，按依赖排序）

### 批 A：skill references（吸收批内容 + IoT + theming）

| # | 任务 | 文件 | 验收 |
|---|---|---|---|
| A1 | C1 bullet（Row 内 Text ellipsis 须先包 Flexible/Expanded）→ **Row/Column 主轴分配节**；C2 bullet（无断词点长串 UAX#14 softWrap 不生效，逐字符 Wrap / 零宽断点）→ **滚动与溢出节**；第 3 bullet 实证指针（复盘链接）随各条 | `references/layout.md` | 两节各有新增 bullet；含 UAX#14/ellipsis 关键词；出处带复盘指针 |
| A2 | C3 坑卡（Riverpod defunct 三板斧） | `references/platform-pitfalls.md` 三方依赖分节 | 字段全；区间按查证（查不到如实标） |
| A3 | C4 坑卡（系统字体回退不可信） | 同上「Flutter SDK」分节 | 出处含 bbf976e42/0508ac9b5/656558508 + flutter#154166/#145069 |
| A4 | 新建 `references/iot-devices.md`（铁律 7 条自 backend **:45-53** 迁入 + 8 类设备分节框架；实证 3 处带 commit 出处，其余标待沉淀） | `references/iot-devices.md` | 铁律 7 条与 git 删除版一致 |
| A5 | theming.md google_fonts 分场景：离线/内网设备（POS/KDS/工控）禁运行时拉取，字体打包 assets；联网消费 App 可用 | `references/theming.md` | 「离线」「打包」关键词在 |
| A6 | SKILL.md 按域取用表 +1 行（IoT/外设 → iot-devices.md） | `SKILL.md` | 表含新行 |

### 批 B：模板与清单层

| # | 任务 | 文件 |
|---|---|---|
| B1 | app §七内新增 `### 屏幕适配方案【复制后三选一】`（A1 三档断点默认 / A2 缩放 / A3 自定义）；plugin §八 +1 行指针（涉 UI 参考 app §七）——**仅 app 落正文，不新增 ## 节** | `app/CLAUDE.md` + `plugin/CLAUDE.md` |
| B2 | app 8.1 内新增 `### 字体策略（公共·必选）` 四要素（打包/注入点/fallback/转图，~12 行）；plugin 不落 | `app/CLAUDE.md` |
| B3 | 文件头条替换「默认跟随项目现状」版：app:228（删 @Email/@Company 五件套）+ plugin:254（@Author 文档头改同语义） | 双模板 |
| B4 | checklist :47/:80 对齐（按 §七 对号审查）+ 专项新增「统一注入点」「fontFamilyFallback」两条 | `checklist.md` |
| B5 | checklist R1-R3 半句并入（9/4/8 条）+ **R4 全文条件条**：「涉复刻 / 跨设计稿折算任务：一个控件查两遍（尺寸折了吗？字号跟了吗）；像素对齐对齐的是信息与视觉规格，不是布局机制（滚动区内外、撑底方式按目标屏取舍）；方案 6.2 用例表已含折算对照的直接复跑」+ 差集标注 4 映射 | `checklist.md` |
| B6 | backend.md 铁律节（:45-53）→ 一行指针；error.md:26 **通用表述保留**，仅追加指针半句「外设长流场景细化见 skill `references/iot-devices.md`」（通用坑不整体引流到 IoT 文件） | `agents/backend.md` + `agents/error.md` |

### 批 C：收口

| # | 任务 | 文件 |
|---|---|---|
| C1 | CHANGELOG 0.6.0（内容账本 + minor 口径说明：模板交互环节变更） | `CHANGELOG.md` |
| C2 | 双 json bump 0.6.0 | `.claude-plugin/*.json` |
| C3 | fork-coverage §六节题改「边界记账」通用名 + 追加 0.6.0 行（纯内容+模板交互；S13/S14 处置记录；0.6.0 边界：本批通用机制不反哺 crules） | `docs/fork-coverage.md` |
| C4 | 验证跑全（判据组 + `bash scripts/test-self.sh` **人工必跑**——孪生守护真实断言节序号集 + §一/三/四/五/六 条数，`###` 子节不应触发；炸则回批 B 修） | — |

## 验收判据汇总（交付前一次跑全）

```bash
grep -c '最后核验' skills/flutter-rules/references/platform-pitfalls.md   # ≥7
grep -c '@Email' app/CLAUDE.md plugin/CLAUDE.md                            # 0 0
grep -c 'screenutil' checklist.md                                          # =0（对号措辞不点名包）
grep -c '统一注入点\|fontFamilyFallback' checklist.md                      # ≥2
grep -c '设备通信铁律' agents/backend.md                                    # 0（指针行在）
grep -c 'iot-devices' agents/error.md                                      # ≥1（指针半句在）
grep -c '一个控件查两遍' checklist.md                                       # =1（R4 全文在）
grep -c '金额展示铁律\|窄屏极值\|pathspec' checklist.md                     # 各 =1
bash scripts/test-self.sh                                                   # PASS（断言数随批更新）
```

## 风险与回退

- `###` 子节不入守护节序号集——按 tt_sec 实现推断，C4 人工跑 test-self 实证；若实现实际扫全级别标题，回批 B 改「双侧同加 ## 节 + plugin 节内容写 N/A」
- 坑卡链接可达性由 CI 链接检查步看守（C3/C4 issue 链接）
- 回退：批 A/B/C 文件级 git 回退各自独立；CHANGELOG 顶部条目随批回退
