# 复盘 · 2026-09-24 · 硬件外设批1 沉淀（打印机 / 扫码枪·扫码盒 / 钱箱）

> 状态：已落地（待 commit 授权）· 关联方案：[方案-2026-09-23-硬件外设知识沉淀.md](方案-2026-09-23-硬件外设知识沉淀.md)（v6 已确认）· 素材源：saas-cashier（纯只读，master-fix2108 线）
> 落盘面：`skills/flutter-rules/references/iot-devices.md`（58 行 → 本次扩写）+ SKILL.md 索引行 + agents/backend.md 分节列举同步。

## 结果一句话

批1 三域 34 卡全过（含 5 处现值修正）+ 补 1 新卡（Android 蓝牙分块实测簇，检验轮发现漏采）= **35 卡入分发面**；卡19（MQTT 失订）毙出留网络域指针；RS232 标「无实证不预置」；未合入分支两笔料仅登记（本文件 §登记处）。

## 三轮检验账（需求方三次质询驱动）

| 轮 | 触发 | 结果 |
|---|---|---|
| 一 | 「git 日志和本地代码」 | 哈希存在性 → 语义核（subject/日期 vs 卡文主张）；纠出卡7 平台误归属（Windows → 实为 Android Kotlin UsbConn.kt） |
| 二 | 「确定？」 | 自查盲区：撤回对 D4 150ms 的「编造」指控（注释在 cd88b2717 diff 逐字在，检索败于子串不匹配「充足**的**缓冲」+ 误读 -S 输出）；计数纠错 63→91 引用锚；补 142nd 哈希 |
| 三 | 「再次检验下，不要再犯类似错误」 | 程序化全量：142/142 哈希标题核对、8 处引语 diff 逐字、18 项现值断言复跑（3 异常全解：D6 ✓ / D1 出处指针过期 / 卡12 现值）、覆盖计数 694/223 与自报一致；**新发现 Android chunkSize 簇漏采** → 需求方裁补卡 |

错误类清点（本轮自我纠偏的类别，供后续批次采证 subagent 提示词引用）：证据面未穷尽即下结论、误读命令输出、子串不匹配假阴性、手数错误、shell 陷阱（zsh 未引用通配符 / `while read` 吞末行）。

## 裁决记录（2026-09-24，AskUserQuestion 四问）

1. **34 卡总裁决 = 全过**（含现值修正与脱敏稿）。
2. **卡19 MQTT 失订 = 毙出 iot-devices.md**——归网络域既有口径，打印专项留一行指针（锚 12365c95a / 693904bbb）。
3. **分支两笔料 = 仅登记**（见下节，不合线不入卡）。
4. **Android 蓝牙分块簇 = 补为蓝牙节新卡**（fef75d305 / e58120e9f 领衔，与卡2 iOS MTU、USB 流控构成三平台对照）。

RS232 串口：无实证料，按方案 G2 默认标「无实证不预置」（批2 电子秤采证后处置）。

## 登记处（不入卡，备后续）

**未合入 main 线的分支料**（需求方裁「仅登记」，合入后随批补）：
- `9b90475a6`（fix/usb-printer-deferred-disconnect，2026-09-23）：佳博 USB 标签机连发漏打 deferred disconnect 修法；配套设计文档 `doc/usb_printer_miss_print_fix_design.md`（312 行，可复核锚）——与卡12（TSPL 关端口时机）同域延伸候选。
- `bc73779ce`（dev_windows + fix/windows-usb-printer-cross-print）：Windows 串纸问题，native printRaw 局部句柄方向。

**HEAD 硬件提交全量过目的无料处置**（D5 纪律补登）：04c3265cf（customImg 彩色 bug 治理，roadmap 文档继承）、8f7d1d2cc（延时优化，已入 D4 值演化注记）、61ce2bf24（蓝牙 TSPL 文字打印 feat）、M-x/C-x 评审系列（e56f0e6f1 / 9df382696 / db03889e0，bluetooth_print_log_design 评审文档继承）、docs/治理系列与 perf/插桩类（roadmap 继承）、UI 误命中类——均无独立可迁移坑。

**在 HEAD 未被采集报告引用但检验轮已核**：fffe5631f（mqtt 重构，扫码报告无料清单内，grep 命中系 UserInfoDrawer）。

## 落盘修正清单（对采集报告的修正，已生效于分发面）

| # | 修正 | 依据 |
|---|---|---|
| 1 | 卡7 平台归属：Windows → Android（Kotlin UsbConn.kt:32 DEFAULT_TIMEOUT_MS=5000L） | f52f68889 diff 实读；Windows cpp 零命中 |
| 2 | D4 值演化：150ms（历史）→ 移除 → 现行 Android 侧 100ms（`if (Platform.isAndroid)`） | cd88b2717 / 8f7d1d2cc / base_printer.dart:244-248 现读 |
| 3 | 卡12 现值：600/800ms 常量已随重构消亡，现 `_kUsbRetryDelays=[500,700,900]` | usb_printer_manager.dart:534 现读 |
| 4 | D1 出处指针：printer_help.dart 现仅 227 行薄助手，success 闸实际在 printer_manager.dart | 现读 + grep 零命中 |
| 5 | 卡17 形制：现行 `scale: imgScale` 变量化（receipt_temp_widget.dart:212），守护测试机制级在位 | test/pub/pic_custom_img_scale_dpi_test.dart 现读 |
| 6 | 幽灵锚 173e731c1 → 173e731c8（方案 I8 既定，随批1 修正） | git cat-file -t |
| 7 | 卡9 残余风险（socket 错误分支不回队）确认为现网现状，入卡文 | printer_task.dart:404-426 现读 |
| 8 | 卡18 钉版实在 plugin/esc_pos_utils-1.1.0/pubspec.yaml:16（^1.18.0），根 pubspec 放宽为区间 | 现读 |

## 机械验证（T 面）

- **T2**：【待项目沉淀】占位 5 → 1（余副屏一条，批2 处置）；文件 58 → 本次行数见 commit。
- **T4**：`git diff --check` 绿；指针面（SKILL.md / backend.md）同步无死链。
- **T6**：入卡全部锚在本检验链内逐一 `git cat-file -t` / `git log -1` 核过（142/142 含引用锚 91 个 subject/日期比对；新卡簇 5 哈希出自 git log 全量列表本身）。
- **脱敏**：落盘文无一商户实名 / 工单号 / 内网 IP / flowId（品牌机型名按 I10 保留为主检索键）；批3 V5 终验 grep 兜底。
- 版本三件套：本批不触（D9：三批收口为一个版本批）。
