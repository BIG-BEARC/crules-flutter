# 评审记录 — plan · 升级合并体验优化（2026-09-24）

> 评审对象：docs/方案-2026-09-23-升级合并体验优化.md（v1）
> 评审形态：plan-reviewer 完全隔离评审（只读不改），主控对改变结论的事实断言全部二次实测后吸收——其中一处评审自报事实被主控实测推翻（见 #2 裁定）。
> 结论：甲可实施（须吸收 #3/#6/#7/#8/#9 修订）；乙现文本不可作裁决输入（#5 缺口）。后续主控提出丙（双文件分离）替代乙，需求方 2026-09-24 裁「甲立批（带自存 base）+ 终态丙」；同日主控实测解锁 D（绝对路径 @ 导入 `-p` 实证）并补充枚举设计空间（B'/D2/E/F/G 否决），需求方再裁终态改 D——两轮裁决与证据链见方案 v2 §3/§4。

## 逐条发现与主控核验

| # | 评审发现 | 主控核验 | 处置 |
|---|---|---|---|
| 1 | install.sh 五处机制描述属实（:50-62/:67-82/:40-46/:99-126） | 采信（与主控读脚本一致） | 无须改 |
| 2 | 🔴「cache 只剩 1.0.40，base 前提已失效」 | **推翻**：主控两次 `ls` 实拍 cache 根 17 个版本目录俱在（1.0.4–1.0.40，含 1.0.6/1.0.25），git tag 亦在；本轮合并 base 即取自 cache 且成了。评审此条未附原始输出 | 「已失效」不采；其底层风险（cache 保留策略不可控）成立 → base 自存加固（v2 §2），经 D-5 裁决采纳 |
| 3 | 🟡 §0 两处数字口径混用（162 vs 128） | 属实：162/318 = diff 总输出行，128/250 = 净改动行（`^[<>]`），同节混用 | v2 统一为净改动行口径 |
| 4 | 「各仅 1 冲突块且同形」属实（merged.md:312-349 / :302-392） | 采信（实物在 /tmp 两目录，主控复核同） | 无须改 |
| 5 | 🔴 乙「区外直替」会覆盖 §七 中部定稿（saas-suite:193「已定稿 2026-09-20」、light:193-222 实查；甲三向合并反而零冲突保住） | 属实，本评审最重发现 | 乙弃用，改丙（v2 §3）——丙中 §七内容随迁移搬入项目文件，缺口结构性消失 |
| 6 | 🟡 写回护栏缺失：未规定写回前备份 / 冲突未解禁更新戳（否则 check-imports:15 假绿）/ 冲突标记窗口警示 | 属实 | 三条护栏写入 v2 §2 |
| 7 | 🟢「install.sh 在 git 仓工作流内」措辞不准（merge-file 只需 git 二进制） | 属实 | v2 措辞改 |
| 8 | 改动面遗漏：install.sh 尾注「下一步④」与 init.md 的 .new 叙事需随批改写 | 属实 | v2 改动面补 |
| 9 | 🟡 fixture 固定 /tmp 路径不抗并发（项目记忆 multi-session-same-repo-collision） | 属实 | v2 验证设计明确 mktemp |
| 10 | D-2/D-4 无异议；ours/union 免标记策略劣于「宁冲突勿错并」 | 采信 | — |

## 更优替代评估（评审第二问 + 主控追加）

- 评审三案（免标记自动落盘 / 维持现状 / 先乙后甲）均被否，理由在册。
- 主控追加**丙（双文件分离）**：依据=模板家族已生产用 @ 导入（app/CLAUDE.md:333 `@.claude/memory/NAVIGATION.md`、消费工程 light:292 现存），零新机制承重；乙的 #5 缺口结构性消失。需求方 09-24 首裁采丙替乙。
- 主控再追加**丁/D（指针引用）**：绝对路径 @ 导入 `-p` 实测生效（mktemp 对照实验命中标记串）后解锁，拷贝漂移结构性归零；需求方同日再裁终态改 D，丙降为回退形态。D2（稳定路径）/E（hook 注入）/F（subtree）/G（组装）逐一定性否决，台账见方案 v2 §3.4。
- 指针方案未验面（子代理继承、/context、交互模式）列为 D 实施前必测三件，任一不过退丙。
