# 实施计划：1.0.0 批（P0 独立成库——斩断与 crules 的联系）

> 状态：**已实施**（2026-09-08，批 A/B/C 全部落地；随批修 1 项：语义闸③裸 hive 检查收窄整词匹配——B4 归档引入 archive 字样暴露既有闸词级误配盲点。test-self 17 PASS / 复扫白名单零清单外命中）· 依据：[外审-2026-09-08](外审-2026-09-08-0.6.2全面评审与社区对标.md) §6 P0（已裁决采纳）· 版本口径：**1.0.0（major·身份变化）**——独立成库里程碑，宣示自持；非破坏性 API 变更，1.0.0 语义取「成熟度宣告」而非 breaking。
> 裁决前置：deny-list 处置选 **方案 a Vendor 终态**（本地 75 fixture 红队样本库已足保质量）。
> 引用面摸底（2026-09-08 全仓 grep）：活机制引用 8 处必改；**史实性引用保留**（CHANGELOG 历史条目 / checklist·进阶 的「fork 自 crules v74」出处标注 / docs 过程文档——出处是溯源资产，斩断的是依赖不是历史）。

## 任务清单（批 A 斩机制 → 批 B 文档 → 批 C 收口）

### 批 A：机制斩断

| # | 任务 | 文件:位置 | 改法 | 验收（可机检） |
|---|---|---|---|---|
| A1 | 删 deny-list SYNCED-FROM 戳（Vendor 终态） | `hooks/deny-list.py:2-3` | :2 戳行删；:3「crules 破坏性命令 deny-list」→「crules-flutter 破坏性命令 deny-list（源自 crules v74 fork，2026-09 起自持演进——安全修复本地 fixture 库回归）」 | `grep -c 'SYNCED-FROM' hooks/deny-list.py # =0` |
| A2 | 同上 test_deny_list | `hooks/test_deny_list.py:2` | 戳行删，改一行自持声明 | `grep -c 'SYNCED-FROM' hooks/test_deny_list.py # =0` |
| A3 | CI 删同步比对步 | `.github/workflows/ci.yml` | 删 :59-66「deny-list 同步比对」整步；:1 头注释去「crules 单一权威看守」；:24 name「fork 自 crules」→「源自 crules fork」 | `grep -c 'BIG-BEARC/crules/main' ci.yml # =0`；`grep -c '同步比对' ci.yml # =0` |
| A4 | hooks 头注释去母版名 | `hooks/pending-updates.py:2`、`hooks/stop-reminder.py:2` | 「crules 记忆库…」→「crules-flutter 记忆库…」 | `grep -l '^# crules 记忆库' hooks/*.py # 空输出` |

### 批 B：文档与措辞

| # | 任务 | 文件:位置 | 改法 | 验收 |
|---|---|---|---|---|
| B1 | README 定位句改独立 | `README.md:3` | 「通用协作层 fork 自 crules 基线 v74-fork-base…此后独立演进、互不依赖（唯一例外见…）」→「**独立**协作规则 plugin（源自 crules v74 fork，2026-09 起完全独立、无母版依赖；fork 史见 CHANGELOG）」 | `grep -c '唯一例外' README.md # =0` |
| B2 | README 删「与 crules 的关系」节 | `README.md:118-129`（:131 起 = `## 维护`，**勿带删**；按节题锚点操作防行号漂移） | 整节删（fork 基线/边界判据/跟不跟查表/同步义务/双包共存五块——依赖已斩，全节失义）；「双包共存」中「同项目二选一勿双装」一句并入「怎么用」安装段保留（自包使用纪律仍在） | `grep -c '与 crules 的关系' README.md # =0`；`grep -c '二选一' README.md # ≥1`；`grep -c '^## 维护' README.md # =1` |
| B3 | README 维护节三行清理 | `README.md:133-135` | 「纪律继承 crules v31」→「纪律承本仓 v31 先例」；「crules 的五维雷达…不复制」→「五维雷达/评审轮次体系不引入（治理从简决策仍有效）」；「crules 的独立 subagent 复审模式」→「独立 subagent 复审模式」 | `grep -c '五维雷达' README.md # ≥1`（决策保留）；`grep -n 'crules' README.md` 仅剩 :3 溯源句 |
| B4 | fork-coverage 归档 + 活指针处置 | `docs/fork-coverage.md` → `docs/archive/`；`app/CLAUDE.md:262` | 移入 `docs/archive/`（Y10 首件；install.sh 落位清单**不含 docs/**——评审核实，零安装影响）；**app:262「适用面判定见 `docs/fork-coverage.md` §三」断链指针**改「适用面判定见仓内 `docs/archive/fork-coverage.md`」（消费项目本就无 docs/，降为仓内归档措辞）；进阶两处文字提及无路径链接，史实留 | 文件在 archive/；`grep -n 'fork-coverage' app/CLAUDE.md` 指向 archive/ |
| B5 | 措辞残留清理（扩：双模板 v77 跟随改承先例） | `commands/init.md:67`、`scripts/release.sh:10`、`app/CLAUDE.md:105/107/112`、`plugin/CLAUDE.md:98/100/105`、`进阶/记忆库体系.md:139` | 「crules 老项目纪律同款」→「本仓老项目纪律」；「纯名 "crules" 会报 not found」→「纯名会报 not found」；六条「（跟随 crules v77）」→「（承 v77 先例）」——活纪律的规范性引用改自持措辞（类 B3 处理）；:139「crules 源仓库中 memory/ 为模板」→「本仓 memory/ 为模板」（事实修正——本仓已是源） | `grep -c '跟随 crules' app/CLAUDE.md plugin/CLAUDE.md # =0`；`grep -c '承 v77' app/CLAUDE.md # =3` |

### 批 C：收口

| # | 任务 | 文件 | 内容 |
|---|---|---|---|
| C1 | CHANGELOG 1.0.0 里程碑条（独立成库：方案 a Vendor 终态 + 斩断清单账本 + **史实保留清单固定**：install.sh:2 / 双模板 ：3 / 进阶×5 的 :3 fork 声明 / checklist:3「fork 自 crules v74…不依赖 crules 包」/ 进阶/工程化流程:160——出处史实，永不改写） | `CHANGELOG.md` |
| C2 | 双 json bump **1.0.0** + README 横幅同步 1.0.0（保留「当前状态：1.0.0 …」前缀格式——语义闸①正则锚点） | `.claude-plugin/*.json` + `README.md:5` |
| C3 | 验证全跑：批 A/B 全部 grep 判据 + `bash scripts/test-self.sh`（17 PASS 预期不变）+ 复扫**白名单式**：`grep -rn 'crules' --include='*.md' --include='*.yml' --include='*.py' --include='*.sh' . | grep -v crules-flutter | grep -v docs/ | grep -v '\.git/'` 命中集须**逐条等于 C1 史实保留清单**（fork 声明类），不得出现清单外新命中 | 复扫输出 == 白名单预期集 |

## 风险与预退

- **A1/A2 删 :2 戳行后 CI :64 的 `sed '2d'` 逻辑失效**——A3 同批删整步，顺序无先后问题（同批原子）；若分批执行须 A1-A3 不可拆分。
- **B2 删节后 README 锚点引用**：外审/计划文档中「README §与 crules 的关系」指针失效——过程文档不改（史实），仅 CHANGELOG 1.0.0 条内注明。
- **消费项目升级影响**：纯本仓侧改动（hooks 行为不变、deny-list 逻辑零改动），plugin update 后消费侧无感——风险最低档。
- test-self 17 断言不含 SYNCED-FROM 相关检查（摸底确认），删戳不炸自测。

## 评审裁决记录（v1 → v2）

| # | 评审项 | 裁决 |
|---|---|---|
| 🔴1 | C3 行级 `grep -v crules-flutter` 判据自冲突（「源自 crules fork」「跟随 crules v77」等行必命中白名单词；含 crules-flutter 的行反被静默滤掉） | C3 改**白名单式**：复扫命中集须逐条等于 C1 固定的史实保留清单，零清单外命中 |
| 🔴2 | B4 归档致 app/CLAUDE.md:262 活模板断链指针未处置 | B4 扩子任务：:262 改指 `docs/archive/fork-coverage.md`（消费项目无 docs/，降仓内归档措辞）；进阶两处文字提及史实留 |
| 🟡3 | B2 写「:118-131 整节」会误删 `## 维护` 节题（实测节界 :118-129，:131=维护节题） | 边界改 :118-129 + 节题锚点操作 + 验收补 `^## 维护 =1` |
| 🟡4 | 摸底 8 处不完备：双模板 6 条「跟随 crules v77」系活纪律规范性引用（非史实）未裁决；记忆库体系:139「crules 源仓库中 memory/ 为模板」事实过时 | 并入 B5：六条→「承 v77 先例」；:139 事实修正；其余 fork 声明类在 C1 列史实保留清单固定 |
| 评核实 | A1-A4 原样可执行（戳行/步边界 :59-66/删步无级联）；install.sh 不复制 docs/（B4 零安装影响）；marketplace.json source "./" 无母版引用；test-self 无 SYNCED-FROM 断言；语义闸①横幅前缀格式保留即过 | 采纳备查 |
