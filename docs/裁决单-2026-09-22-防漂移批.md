# 裁决单：外部可维护性评审核实 + 防漂移批（hooks 收编 / 清单派生化）

> 状态：**待裁决** · 日期：2026-09-22
> 来源：外部可维护性评审五弱点主张（bus factor / 双模板 / 三处人肉同步存量）→ 本会话逐条实测核实 + 九轮「还有更好方案吗」追问收敛出的终版方案。
> 队列与时序（2026-09-22 跨会话协调已确认）：1.0.37 遵守度批 = 第三方会话在途；1.0.38 = crules-flutter-cd 易用性批（50/0 绿、已列禁碰清单）；**本批若获裁决走 1.0.39**，落笔前与两批改动面逐行核对（本仓 test-self.sh / README.md / ci.yml 为三方共同高频落点）。

## 0 外部评审逐条核实结论（九轮实测留痕）

| 主张 | 核实 | 依据 |
|---|---|---|
| ① bus factor=1 无缓解 | **成立** | 全部提交单一作者（92→94 枚复核期间仍 +2，作者集恒 = chuxiong/cx 同一人；≈3.7 周）；CHANGELOG 黑话密度属实 |
| ② 双模板 ~200 行共享仅 4 块入 canonical | **大体成立** | 公共子序列 219 行；但「plugin 单方面删了『任务同时可理解为…』句」**失实**——git log -S 全史该句从未存在于 plugin 侧（系 fork 时即只写入 app 侧） |
| ③ py/ps1 双源靠人同步 | **夸大后基本成立** | 有单源 fixture 双驱动（1.0.35 又强化）；真正无闸的是下面的代码复制 |
| ③' 两 hook 的 find_project_root/sid 各自复制 | **成立且低估** | 实测顶层同名符号复制对（等值性逐个断言，2026-09-22 复核）：**x3 三重复** find_project_root / MAX_SID / _hook_utf8_streams（3 文件全等）＋ **x2 二重复** sid / mem（2 文件全等）——compliance-audit.py 落地当日把三对抄成第三遍；_hook_utf8_streams（stop↔deny-list↔compliance）连同源注释都没有。root 在 compliance 侧系 MIXED（`find_project_root(cwd)` 带参调用式，语义合法分叉），排除。评审说的「两处」实为五对三文件 |
| ③'' release 与 CI py_compile 清单已在漏 | **曾经成立、已修一半** | release 侧 1.0.34 已删手挑清单；**CI 侧仍手写 9 文件**（ci.yml:29），1.0.37 又手改过一次 |
| （评审未提，本会话新发） | 清单复制同族病灶 | AV 守卫手列清单（test-self:374）、install.sh 文案、README/help 计数、CHANGELOG——第 4 个 hook 落地一次 = 人肉同步 5+ 处（1.0.37 实证交税）；closing-audit↔compliance-audit 判据常量又新增第 7 对双源（当日增量，非存量） |
| 「不引共享模块」是已登记决策 | **幻影决策（本会话一度误信）** | 全仓存证 = 三处互指注释本身，CHANGELOG/提交信息零记录；install.sh 无单文件复制逻辑，「独立分发」系伪命题；需求方已确认非本意（2026-09-22「1.不是」） |

## 1 须需求方裁决

| 编号 | 条目 | 选项 | 主控建议 |
|---|---|---|---|
| D-1 | **核心路线**：hooks 复制病治法 | (a) 不加闸维持现状（注释级承诺；反证在涨：6 天内 2→3 文件、当日新增第 7 对双源）；(b) 只加检测闸（AST 等值/glob 绊线，双源保留）；(c) **`hooks/_common.py` 收编**——3 符号一份、4 生产 hook 改 import、漂移构造上不可存在；(d) canonical 代码生成器（render-blocks 扩面——为 40 行引入整套工具面，收益/侵入不赢，已否） | **(c)**——需求方已确认「独立分发」非本意；爆炸半径一坏多坏与现状同级（今天 closing-audit 判据若改错也全红），fixture 真调进程 + tag 必达点已在 |
| D-2 | **防再增殖绊线形态** | (a) 手挑名单（已被证伪：名单本身是漂移源，本会话两轮数错过数）；(b) **glob 绊线**：跨 ≥2 生产 hook 同名顶层符号 == 0（排除 test_/_common），新文件自动入网；(c) 不锁（靠 D-1(c) 后无源可抄） | **(b)**——约 20 行 heredoc；(c) 的残余风险 = 第 5 个 hook 又抄一遍而没人提醒 |
| D-3 | **清单派生化范围** | (a) 只 CI→`compileall hooks/ scripts/`（一行，根治 ci.yml:29 手列）；(b) (a) + AV 守卫清单改从 hooks.json 派生（注册命令出现的 .py 都要求守卫）；(c) (b) + install.sh/README/help 计数文案随批核对 | **(b)**——(c) 触碰 cd 会话禁碰面（README 环境节等），改随其 1.0.38 落地后核对 |
| D-4 | **往返契约测试**（pending 写→stop 读真调互操作，fixture 临时项目） | (a) 入本批；(b) 次批单独走；(c) 不做 | **(a)**——AST/绊线结构上抓不到「两侧同模漂移」；1.0.30 R5（路径归一写读侧互踩）实证此盲区咬过人；两个既有 test 文件已各自会构造 stdin，只缺互调，且不在两批在途面上 |
| D-5 | **closing-audit 判据双源**（第 7 对，scripts/↔hooks/ 跨分发面） | (a) 本批不动，随**批 3**（探针去留裁决）一并收口——探针本就是 hook 前稿，双文件制系过渡产物；(b) 立即让 closing-audit 从 compliance-audit 读常量（importlib 按路径） | **(a)**——批 3 若裁「探针并入 hook --dry-run」则此对自动消失，现在接线是白工 |
| D-6 | 排期确认 | 本批 = **1.0.39**，待 1.0.37（第三方会话）+ 1.0.38（cd 会话）先后提交；期间本会话不动文件 | **按队列**——三方共享工作树，显式列文件规矩下抢先必互踩 |

## 2 机械处置（获 D-1~D-4 通过后自动执行，不逐条裁决）

- `_common.py` 头部注记收编来源与 1.0.30 幻影决策的勘误出处（防第三例注释再传播）；`compliance-audit.py:10` 的「不引共享模块」句随收编删除。
- test-self :173 计数闸（`hooks ×N == help.md`）口径同步排除 `_common.py`——**已实测该闸会咬**（不排除则 _common 被数成第 5 个 hook，help ×4 当场红）。
- 收编仅涉 3 符号（find_project_root / MAX_SID+sid 净化 / _hook_utf8_streams）；`mem`/`root` 系各 hook 顶层一行调用式、compliance 的 root 实测语义有别（MIXED），**不强归一**。
- 新绊线断言先红后绿留痕（本仓 F3 处置哲学：先加会红的测试再修）。
- 诚实边界随批登记：绊线锁「同名」，改名复制（`find_project_root`→`_root`）结构上抓不到，注释留作人读线索；py/ps1 跨语言双源不在本面（fixture 单源已锁）。

## 3 执行清单（D-1(c)+D-2(b)+D-3(b)+D-4(a) 全通过时）

| 文件 | 动作 |
|---|---|
| hooks/_common.py | 新建（3 符号 + 来源注记） |
| hooks/pending-updates.py / stop-reminder.py / deny-list.py / compliance-audit.py | 删各自拷贝、改 sys.path 三行样板 + import |
| hooks/test_pending_updates.py / test_stop_reminder.py | 加写→读往返契约用例（D-4） |
| scripts/test-self.sh | 加 glob 绊线（D-2）；AV 清单改 hooks.json 派生（D-3b）；:173 计数排除 _common |
| .github/workflows/ci.yml | py_compile 手列 9 文件 → compileall |
| CHANGELOG.md / 双 json / README 横幅 | 1.0.39 随批 |
