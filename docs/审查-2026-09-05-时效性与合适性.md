# crules-flutter 时效性与合适性审查（2026-09-05）

> 状态：审查报告（待裁决，未动任何规则文件）· 方式：双子代理独立审查（SKILL.md / 模板与配套件）+ 主控逐条复核原文 · 复核方式：🔴 与 🟡 事实类条目均以 `sed`/`grep` 对原文逐字核对；插件 skill 引用查本机 dart-flutter 插件实际目录；🟠 为取舍裁决项非事实项

---

## 一、确定的错误（🔴 建议优先修，原文已逐字核对）

| # | 位置 | 问题 | 建议修法 |
|---|---|---|---|
| S1 | `skills/flutter-rules/SKILL.md:460-461` | 句子损坏：「For cached images, use NetworkImage a package like `cached_network_image`」——缺词且语义矛盾（`NetworkImage` 恰不缓存） | 改「use a package like `cached_network_image`」 |
| S2 | `skills/flutter-rules/SKILL.md:487-488` | Material `TextField` 无 `placeholder` 参数（是 `CupertinoTextField` 的）——照写编译失败 | 改 `InputDecoration.hintText`（或注明 Cupertino 语境） |
| S3 | `SKILL.md:134` vs `:341-345` | 两套 logging 建议打架：Code Quality 节「Use the `logging` package」vs Logging 专节「Use the `log` function from `dart:developer`」，无归口 | 归口一处（Logging 专节为准，L134 留指针），或写明分工 |
| S4 | `SKILL.md:503-507` | 主题切换示例用 `ChangeNotifierProvider`（provider 包 API），与全文三处「默认内置、三方仅明确要求时引入」立场矛盾 | 改 `ValueNotifier` + `ValueListenableBuilder`（与 L228-229 自荐一致） |

## 二、时效性存疑（🟡 需 web 查证后定）

| # | 位置 | 问题 | 查证点 |
|---|---|---|---|
| S5 | `SKILL.md` iOS 26.x 坑卡（~L834-837） | 版本锚停在「官方文档 3.47.2 快照」，若当前稳定版已 4.x，「状态：未修复」结论与区间需重核 | 当前稳定版下 liquid glass / affected 版本 |
| S6 | `SKILL.md` Android 基线坑卡（~L841-850） | 锚点停在 API 29/33；疑似缺 targetSdk 35+ 时代强制项：edge-to-edge、photo picker 政策、16KB page size（Android 15+） | Google Play 当前 targetSdk 要求 + 三强制项条款 |
| S7 | `app/CLAUDE.md:182/192`（两模板 §七预设 A/B） | Hive 列默认本地存储——社区公认长期停滞，2026 新项目默认有踩坑风险 | pub.dev hive/hive_ce 当前维护状态 |
| S8 | `app/CLAUDE.md:183` + `agents/i18n.md:12` | easy_localization 与官方 gen-l10n 并列等价——前者长期未实质发版 | pub.dev 最新发版时间 |
| S9 | `SKILL.md:179` | `compute()` 应改推 `Isolate.run`（compute 已是其后向包装） | 官方文档当前口径 |
| S10 | `SKILL.md:425` | `google_fonts` 无条件推荐偏旧（运行时拉字体/离线回退争议） | —（裁决即可） |
| S11 | `SKILL.md:677` | WCAG 2.1 → 2.2（对比度数值未变，仅版本号旧） | — |

## 三、内容放错层 / 源项目渗漏（🟠 方向裁决）

| # | 位置 | 问题 | 选项 |
|---|---|---|---|
| S12 | `SKILL.md:42-80` | 「方案骨架」「配图约定」两节是通用文档规范，与 Flutter 零关联，随 skill 无条件加载占上下文 | a) 上移 crules 母版 / 独立 skill，本处留指针；b) 保留（plugin 自含完整方案能力）但文档头声明取舍 |
| S13 | `app/CLAUDE.md:227` / `plugin/CLAUDE.md:251` | 文件头 `@Author/@Email/@Company` 公司抬头——saas_pos 惯例渗漏，fork 到其他团队会被当默认规范 | 降为「项目附录可选项」，正文只留「注释放 import 之后」机制要点 |
| S14 | `agents/backend.md:3,44-52` | 「设备通信铁律」7 条（蓝牙 MTU/OTA/配网）占角色卡约 1/3——IoT 域内容渗入通用角色 | 压缩为一行指针（外设项目查 memory/坑卡），通用部分保留并与 error.md 归并重复 3 条 |
| S15 | `SKILL.md:392-400` | 噪声背景/多层投影/glow 审美说教——特定流派偏好非规范，与自家 M3 立场相悖 | 删除或降为「按项目设计规范执行」 |
| S16 | `checklist.md:37-67` | Flutter 专项缺**无障碍**小节（SKILL description 声称覆盖 A11Y，两文件不对齐）；性能仅 rebuild 一条 | 补「无障碍」「性能」两小节 |

## 四、轻微项（🟢 可顺手修）

- `SKILL.md:15-36`：MCP 条件句「if it is available」重复 5 次（2023-24 MCP 提示词直译残留），L11 已有统一降级声明——整节压缩为直接 CLI 命令
- `SKILL.md:90-93 / 222-223 / 253-257`：状态管理「默认内置」立场写了三遍（DRY）
- `checklist.md:43`：`Selector` 是 provider 词汇，与下文「不混用」规则打架——改「按所选方案对号（provider `Selector` / Riverpod `select`）」
- `agents/*.md` 尾行「§2」vs 模板节标题「二、提交策略」——机器检索不一致，统一写法
- `agents/error.md:16`：「编译错误**修复**」与 ：9 硬约束「只诊断不写入」矛盾——改「诊断与修复建议」
- `memory/reference-map.md:7-11`：餐饮 SaaS 专属示例占模板前 1/3（已标【示例】）——压缩为一行占位
- `app/CLAUDE.md:195-199` 预设 C（Provider + Navigator 1.0 + 手写 fromJson）整体偏旧——注明「仅极端克制依赖时选」或路由统一 go_router
- `agents/platform.md:12` 职责五端不含 Web，与坑库矩阵含 Web 行口径不一
- `analysis_options.yaml` 头注补「flutter_lints 大版本升级时复查规则集变化」（需查 flutter_lints 当前大版本）

## 五、审查中撤掉的误报（留档）

| 条目 | 撤掉理由 |
|---|---|
| §十一速查表引用 `dart-use-primary-constructors` 为「悬空引用」 | **误报**——查证本机 dart-flutter 官方插件 skill 目录（22 个），`dart-use-primary-constructors` 真实存在，引用有效 |

## 六、核对无问题项（防误改）

- README / fork-coverage / CHANGELOG 对外声明与实际一致（命令×5、agents×7、hooks×2、版本 0.4.0、fork 基线 ea4d25c 两处一致）
- checklist 与 analysis_options 交叉引用属实（`avoid_print` / `prefer_const_constructors` lint 标注正确）
- flutter_lints + strict 三开关仍是标准形态
- SKILL.md 未发现 `WillPopScope` / `MaterialState` / `primaryColor` 等废弃 API（`WidgetStateProperty` / `ColorScheme.fromSeed` / `ListenableBuilder` 均为现代 API）
- Win7 permission_handler 坑卡字段齐全、核验新鲜（唯一疑问是 Win7 场景 2026 年的普遍性，实证升格+可执行，建议保留）

## 七、汇总

- 🔴 4 条（S1-S4）：无争议 bug，可直接修
- 🟡 7 条（S5-S11）：先查证（估 30-60min web 核实）再改
- 🟠 5 条（S12-S16）：方向取舍，需求方裁决
- 🟢 9 条轻微 + 撤误报 1 条留档

修复范围、批次（是否并入 0.4.1 或独立 0.4.2）由需求方裁决后另行出修订方案。
