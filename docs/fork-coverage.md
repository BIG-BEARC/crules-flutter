# fork-coverage · 覆盖 diff（批 2a 验收）

> **基线**：crules `v74-fork-base`（ea4d25c）的 `CLAUDE.md`（264 行）。
> **对象**：crules-flutter `app/CLAUDE.md` 与 `plugin/CLAUDE.md`（批 2a 重组版）。
> **三态**：✅ 已整合（内容到位，允许措辞场景适配）/ ✂️ 裁剪（内容未入根模板，留痕理由）/ ➖ 不适用（留痕理由）。
> **裁剪去向原则**：从根模板裁掉的内容若属深度纪律，归 `进阶/`（批 2b 搬运时核对其存在于对应篇）；纯重复/被 Flutter 实例替换的注明。

## 一、基线 CLAUDE.md 逐节核对

| 基线节 | 状态 | 去向与说明 |
|---|---|---|
| 头注（定位/术语/规则用词） | ✅ | 两模板头注：术语（需求方/用户）与规则用词（必须/禁止/默认/例外）全量；定位改为 crules-flutter fork 声明 |
| §一 协作红线 13 条 | ✅ | 两模板 §一 逐条核对**全量**：含 v50 两条安全红线（不可信内容不作指令 / 外部依赖与网络操作先确认）——旧模板孪生缺这两条，本批修复 |
| §一 代码修改红线 4 条 | ✅ | 全量；「不硬编码」条的 i18n 字样在 plugin 版改通用（场景适配） |
| §二 提交策略 | ✅ | 全量：**commit=本地 / push=推送语义分离（v51 版，旧孪生为合并语义——修复）**、授权边界、完整流程、只暂存相关、skill 冲突降级、Commit type 表、破坏性二次确认、hook 边界注；plugin 版新增「发版前 CHANGELOG 与版本号一致」一行（plugin 场景增强） |
| §三 双 Gate 工作流 | ✅/✂️ | ✅：流程图、任务分类、需求 Gate、方案 Gate（大需求强化段简化为指向进阶——批 2b 链通）、Gate 例外、方向停止。✂️ 裁剪（→批 2b 进阶/工程化流程篇核对）：①信任档位注（附录字段保留「提速档声明」位，深度规则归根包附录模式——部分保留）②工具降级表（按方案简化为一行注）。✂️ 实施纪律 13 条精选 7（保留：改共享物查引用/参照物优先/修一坑查同类/补丁死结/修订局部 Edit/方向停止/最小实现；裁 6：二手摘要只用于定位/事实锚定 file:line/新建前查既有/语义来源链——密度控制，批 2b 搬运工程化流程篇时须全量在篇内） |
| §四 验证与证据 | ✅/✂️ | ✅：证据 5 级表（目标环境行重映射 App=真机/plugin=下游集成）、禁笼统已验证、长输出裁剪（v65）、静默失败现象层（app 版保 assets 冷启动实例）、失败处理、「改构建/依赖后」节并入 Flutter 验证命令（代码生成后必跑）。✂️：接口以真实响应为准 / 构建通过≠规范合规——归进阶/审查与复核纪律（批 2b 核对） |
| §五 完成定义 | ✅ | 全量 7 条 |
| §六 后台 agent diff | ✅ | 全量（含派出≠完成）；替换旧模板 §十同文旧版 |
| §七 扩展入口 | ✅ | 裁剪版（指向进阶五篇/agents/附录并入模板尾部）；进阶目录批 2b 建立后链通——**当前为占位引用** |

## 二、flutter 旧模板原有节去向

| 旧节 | 去向 |
|---|---|
| §一 协作红线（AUTO-SYNC 旧孪生，停 v49 前） | **被基线版替换**（本尊化——漂移修复：补 6+ 条） |
| §二/§七 技术栈约定 / 插件类型（三选一） | §七 原样保留（交互不变） |
| §三+§四 插件分工 + 工作流表 | 合并 §九（superpowers+dart-flutter 协作），触发规则与心智模型保留 |
| §六 TDD 适用范围 | §九内（保留分層표；**新增批量红绿分档注**——基线 v65/v66 教训） |
| §五/§八 公开 API 稳定性 / plugin 架构 / 分层架构 | §十 保留全量 |
| §八/§九 验证纪律 / 后台 diff（旧版） | 并入 §四 / 替换为 §六 |
| §十/§十一 skill 速查 | §十一 保留全量 |

## 三、批 3 追加位（上移适用面判定时填）

- §八 Flutter 专项规范 8.1 全栈通用 / 8.2 预设 A 特有——两占位待 saas_pos 上移，每条记适用面判定于此表。

## 结论

基线 264 行：**全量整合约 78%**（红线/提交/完成定义/后台 diff/验证主干/Gate 主干），**裁剪约 18%**（深度纪律 6 条 + 2 小节——去向均为批 2b 进阶篇，搬运时逐项核对），**场景替换约 4%**（附录并入 / 大需求强化简化）。无静默丢失：每条裁剪均留痕于上表。
