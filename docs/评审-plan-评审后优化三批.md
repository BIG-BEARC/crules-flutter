# 评审-plan：1.0.0 评审后优化三批方案

> 评审纪律声明：本文件为独立第三方评审记录。plan-reviewer **只评审、不修改**被评审方案、不产替代方案文档、不调用修改工具；评审结论**不经产出者过滤**原样落盘。评审对象：[方案-2026-09-08-评审后优化三批.md](方案-2026-09-08-评审后优化三批.md)。
>
> 本档含**两轮相互隔离的评审**：§A 主会话评审轮（方案文内「评审记录」节全文收录于此，防正文与记录分离失真）+ §B plan-reviewer 独立上下文轮（opus 派发、只喂方案成品）+ §C 两轮合并裁决表（主控汇总，逐项标共识/增量/勘误）。

---

## §A 主会话评审轮（2026-09-08 · 判据全部实跑/grep 复核）

> 总判：**骨架成熟（分批/不裁清单/成本诚实账/回退表齐），但 2 🔴 裁决前必修**——其中 R1 是 0.6.3「基线过期」同款错误模式，R2 是 0.6.7 评审抓到的「白名单判据 vs 实文措辞」首跑即红同款。修后可裁。

### 🔴 必修（2）

| # | 发现 | 证据 | 修法 |
|---|---|---|---|
| R1 | **批一版本号已被占用——方案写在对过期基线上**。方案标注批一 `1.0.1`，但 1.0.1 已随 `59832d8`（docs 清账）发布：双 json / CHANGELOG / README banner 四处一致 1.0.1 | `grep version .claude-plugin/*.json` → 1.0.1；git log 实证 | 批一改 **1.0.2**，§2 D1-D5 批次列 / §4.1 题注 / §5 分发版本行 / §7 四处同步改。**教训重申：定稿前跑 git log + 双 json——0.6.3 案底同款** |
| R2 | **D8 溯源闸首跑即红**（同 0.6.7 评审 🟡 型：硬编码判据 vs 实文）。断言「出处含 http 链接」但现存 7 卡中 2 张不含：`platform-pitfalls.md:25`（Riverpod 卡 `flutter#73000 + riverpod discussion #3043` 纯文本无链接）、`:33`（SP 卡 `实证 saas-cashier af24988e3/4017e21c1/…` commit 哈希——**私有仓不可能有链接**） | `grep -n '出处' platform-pitfalls.md` 全量核对 | 判据改白名单式：「出处行含 http **或** commit 哈希实证引用格式（`[0-9a-f]{9,}`）」——实证卡无外链是合法形态 |

### 🟡 建议修（3）

| # | 发现 | 修法 |
|---|---|---|
| Y1 | O1「设计稿即本方案 §4.14 存档」悬空——正文只到 §4.12，无 4.14 | 补 §4.13 存档小节，或改为「O1 行内即设计稿」注记（五要素其实已在 O1 行内完备） |
| Y2 | D2 效果表述过强：`git diff v$STAMP` 有**两个**静默分支——① SRC 非 git 仓（真实消费者装的是 plugin cache，走此分支，**先于 tag 缺失被跳过**）② 无该 tag（本地克隆）。方案只救② | ①也显式报告（「SRC 非 git 仓，演进比对需克隆路径」），或文案如实限定效果仅覆盖克隆路径消费者 |
| Y3 | D11 配额计数错：「配额 8→9 ≤10 内」——现 **7** 张卡（`grep -c '^### \['` 实测），加一为 8 | 改「7→8 ≤10 内」；此类数字入验收标准，笔误即裁决误差 |

### 🟢 核实通过（背书）

- **N1-N5 五件【事实】全部为真**：无 LICENSE ✓；全仓仅 `v0.4.0` 一 tag + `2>/dev/null` 吞 ✓；hooks.json 固定 `python3` 三处 ✓；CI 链接步 glob `references/*.md`（ci.yml:39 行号精确）✓；distill 无 `disable-model-invocation` 而 init/update-memory 皆有 ✓
- 交叉引用无虚指：layout.md:26「widget 测试锁死」✓；checklist 9 金额展示铁律原文在 :31 ✓；D9 已知候选 app:33「用户可见字符串」（plugin:33 无此词）✓；test-self 17 断言基线实跑全绿 ✓；README:127-130 例行义务三条确可收敛入维护者手册 ✓
- 判断面背书：D3 注记优于补 disable（能发起是特性）/ D5 README 零动防第三份孪生 / D9 两步走且不对称裁决归人（机判不可为，判据是语义）/ D15 明确不做清单 / §8 成本与回退表——方案最有价值的部分，方向正确。

### 裁决建议

1. 修 R1 + R2（版本 1.0.2 / 断言白名单）后逐条裁 §9 Q1-Q6；
2. **Q1 建议 MIT**：定位公库规则资产、复用溢价值最大、revert 干净（§8 已注）；
3. 批一修完即快执（无实质风险），批二 D9 步骤①留需求方裁决窗口，批三 D13 另线不阻塞。

---

## §B plan-reviewer 独立上下文轮（2026-09-08 · opus · 只喂方案成品）

> 核验手段：全量读了方案与被引文件现场（check-imports.sh / hooks.json / ci.yml / release.sh / test-self.sh 实跑 / distill.md / help.md / README / checklist / 双模板 diff / platform-pitfalls 全卡 / SKILL.md / layout.md / error.md / install.sh / CHANGELOG / 外审 0.6.2 全文），并对两处豁免外部事实用 WebFetch/curl 抓取了官方 hooks 页与 plugins-reference 页原文。

### 发现清单（12 项：2 🔴 / 7 🟡 / 3 🟢）

| # | 发现 | 严重程度 | 原因/证据（含验证状态） | 建议（只建议，不改） |
|---|------|------|------|------|
| 1 | **批一版本号撞车：1.0.1 已被占用** | 🔴 | 已验证（`.claude-plugin/plugin.json` version=1.0.1；CHANGELOG 已有「## 1.0.1 · docs 过程文档清账」节；commit `59832d8` 已推 origin/main；README:5 横幅已写 1.0.1）。release.sh 头注纪律明确「update 按版本号刷 cache，同版本不刷新」——批一再以 1.0.1 发布则消费者 cache 永不更新，分发直接失效 | 批一改 **1.0.2**（§2/§4/§5 三处联动）；1.1.0/1.2.0 不受影响 |
| 2 | **D8 溯源结构断言装闸即红，非「既有不变红」** | 🔴 | 已验证（`platform-pitfalls.md` 共 7 卡；Riverpod 卡出处行 :25 与 SP 卡出处行 :33 **均无 http 链接**——纯实证 commit 号/CHANGELOG 指针）。方案断言措辞「其后首个非空列表节含 http 出处」按卡结构锚字段行 → 这两卡当场 FAIL；与方案自设用例「恢复后 PASS」、§5「每条均为新增而非既有变红」、验收判据 1 全绿三处矛盾 | 上闸前先给两卡出处补可点开链接（Riverpod 卡 :25 已有裸 `flutter#73000`/`discussion #3043` 文本，加 URL 即绿；SP 卡为纯内部实证，需裁决「无公开出处的实证卡如何过闸」——这是 D8 设计上未闭合的缝） |
| 3 | 坑卡配额基数错：现 **7** 卡非 8 | 🟡 | 已验证（`grep -c '^### \['` = 7，D11 写「配额 8→9 ≤10 内」，实为 7→8）；≤10 结论不破坏 | 改「7→8」 |
| 4 | 版本语义内部自相矛盾：D4 走 minor 却排在 patch 批 | 🟡 | 已验证（方案 §5 自述「D4 属内容修订…走 minor」，但 §2/§4.4 把 D4 排进批一 patch 位） | 裁决口径：agent 铁律增补记 patch（连 #1 一并改 1.0.2 时顺手定）或 D4 挪批二 |
| 5 | 编号溯源有拔高：「Y11/Y12 已裁归 P3/P4」与映射表自冲突 | 🟡 | 已验证：外审 §7 裁决表**除 P0 外全部空白**（外审 §7 裁决表 :113-127，原档已删、git 历史可寻回）；Y11→P3 的实际依据在 CHANGELOG 0.6.7「缓记：Y11 守护覆盖矩阵归 P3」（CHANGELOG:26），Y12 无任何落池记录。附表「D1-D5 ↔ N1-N5」与同表「D4↔Y5、D5↔Y9」两行自相矛盾（N3/N4 实归 O3/O4 非 D 系）；批三标题「承 P2/P4 已立项」——A7 restoration 在外审归 **P3**（外审:110）非 P4；且 A3/A4 已由 0.6.3 部分收口（CHANGELOG:53-55，performance.md/build-release.md 实存）「已立项未落」表述过头 | 溯源表述降格为「CHANGELOG 缓记口径」；A5（外审 P2 与 A2 合并立项：coverage/mock/runtime 流程卡）未在 D10 覆盖亦无不做声明，补一行归置 |
| 6 | D9 已知候选四项之三真一假 | 🟡 | 已验证（diff `app/CLAUDE.md` vs `plugin/CLAUDE.md`）：app:33「用户可见字符串」硬编码差异 ✅、plugin:49 发版特殊性行 ✅、app:87 设计/编码默认句 ✅ 全部属实；但「§二 plugin『skill 冲突处理』措辞差异」**不成立**——两文件 :48 该行逐字相同（真 §二不对称在 :40/:44/:52/:56-65） | 步骤①人肉 diff 反正会暴露，仅建议候选清单更正，防裁决时被误导 |
| 7 | §5 影响面漏三项「该更新未更新的落点」 | 🟡 | 已验证：① README:5 横幅含「语义闸 **17 断言**」字样，17→21 后横幅漂移——这正是外审 D1 同型坑，且闸不拦（语义闸①只比版本号），影响面表未列「横幅计数同步」；② D6 从 README:127-130 收敛「/context 复测」项时，15K 线**口径段**整段嵌在 :129 内，README:5「归因口径见维护节」指针将悬空（CHANGELOG 1.0.1 节明确该口径「以本节为准」是删档后的唯一承载处）；③ README:16 排查序指针「memory/MAINTENANCE.md（库自身维护）」在新增根 MAINTENANCE.md 后产生同名歧义 | 影响面表补行；②的口径段建议原地留全、MAINTENANCE 侧只收指针（防第三份口径，同 D5 哲学） |
| 8 | D4 豁免 bullet 与 frontend.md:38 既有条款面冲突 | 🟡 | 已验证（`agents/frontend.md`:38「**只允许** watch 自己对应的页面级聚合状态，**或单一强相关状态**」）：新 bullet「选中态/草稿可直接本地组合」与之同节并存即成「只允许单一」vs「可本地组合多个」的表面矛盾——外审 Y5 点名的正是 :38 这条的严苛性 | 改法应为**修订 :38 加例外从句**而非仅 +1 bullet，否则 reviewer 照 :38 仍会 FAIL 掉豁免合规代码 |
| 9 | release.sh usage() 与 tag 子命令的隐性耦合 | 🟢 | 已验证（release.sh:16 `sed -n '2,7p'` 硬编码头注行号）：+`tag)` 用法行若落在 8 行外，help 输出不含新子命令且无断言会抓到 | 落点注记「usage sed 区间同步」 |
| 10 | D2 未提 tag 须 push origin 与既有 clone 的 fetch 语义 | 🟢 | 推断（验证路径：check-imports 跑在消费者本地 clone 的 `$SRC` 上，消费者不 fetch 则新 tag 永不可见，显式报告反而长期挂「比对不可用」） | D2 落点补「git push origin v<ver>」；消费者侧指引 `git fetch --tags` 入 MAINTENANCE 日历 |
| 11 | D3 注记与 distill 自身 frontmatter「用户触发」措辞共存矛盾；行号微偏 | 🟢 | 已验证（`commands/distill.md`:2 description 明写「**用户触发**，支持 --scope」；新注记「故意允许 AI 发起」与同文件 description 打架；frontmatter 实为 1-3 行非 1-4） | 注记同步改写 description 括注 |
| 12 | §3.5 两处外部断言：**复核全部属实** | 🟢 | 已验证（curl 抓 code.claude.com/docs/en/hooks 原文）：PreToolUse 的 additionalContext「next to the tool result」注入 ✅、不返回 permissionDecision 不阻断 ✅、「factual statements rather than imperative system instructions…prompt-injection defenses」✅、「resume replays saved text…timestamps or commit SHAs become stale」✅；plugins-reference：`license` 为 Metadata 表正式字段 ✅、未知字段运行时忽略 + `claude plugin validate` 记 warning（非 error）✅ | 无需改动，D14 三约束/D1 落法成立 |

### 逐维度结论

1. **事实核验**：抽查 20+ 条【事实】断言。属实：N1（remote+无 LICENSE）、N2（check-imports.sh:22-26 精确、全仓仅 v0.4.0 tag、0.5.0+ 静默失效逻辑成立）、N3（hooks.json:9/21/32 行号精确）、N4（ci.yml:39 glob 属实）、N5（distill 无 disable 而 init/update-memory 有，frontmatter 行号微偏见 #11）、test-self 17/17 实跑复现、memory 8 模板、README:127-130 行号指对三义务、checklist 条目 1「跨源组合」措辞、layout.md:26「widget 测试锁死」精确、help「hooks ×3」与闸②口径、D9 候选 3/4、app:33/:87 定位。失实：#1 版本占用、#2 两卡无 http、#3 配额基数、#6 候选第四项。
2. **可行性**：D2 case 结构兼容（现 `*)` 分支收版本号，`tag)` 前插即可，报错/幂等用例可造红）；D7 矩阵双向断言 shell 可行；D9 豁免行过滤+diff 可行；D10 新文件天然入 CI glob。**不成立两处**：D8 按方案措辞装闸即红（#2）；D4 只加 bullet 会与 :38 冲突（#8）。
3. **边界/风险遗漏**：#1（撞版本）、#7（横幅计数/15K 口径悬空/MAINTENANCE 同名歧义三漏项）、#9/#10/#11；批一验证引用批二矩阵为时序小疵（D3「矩阵行人审」、D4 同理——矩阵 1.1.0 才存在，建议措辞改「批二盘点时补登」）。§5 其余面（常驻 token「无新增 @import」、.new 伴生提示、消费者零模板变化）核验属实。
4. **更优解**：#2 最简消解 = 两卡出处行补 URL（Riverpod 卡行内已有裸 issue 号，成本一行），比放宽断言语义更保「可点开复核」目标；其余无明确更简路径。
5. **范围**：未见夹带——D14 留池、D15 不做清单边界干净；D1 LICENSE 属 N1 缺口修复且已设 Q1 阻塞裁决，不算越界。唯 A5 归属需一句归置（并入 #5）。
6. **编号溯源**：Y5/Y9/A1/A2/A4/A7/Y11 各条内容映射经原文对照属实；失实点集中在**状态声称**（#5：裁决表空白却称「已裁」、D1-D5↔N1-N5 自相矛盾、A3/A4 无视 0.6.3 部分收口）。

**整体结论：有 🔴 须消解**（#1 版本号撞车、#2 溯源闸装闸即红——两者均阻断批一/批二合批验收判据 1，其余 🟡 建议随裁决一并吸收）。

---

## §C 两轮合并裁决表（主控汇总 · 待需求方逐条裁）

> 双轮隔离评审 🔴 完全命中（相互印证 = 共识级）；🟡 增量各四组；主控对 §A 另有三条小疵勘误。

### 必修共识（双轮同判）

| # | 消解项 | 来源 |
|---|---|---|
| F1 | 批一版本号全局 1.0.1→**1.0.2**（§2 D1-D5 批次列 / §4 / §5 / §7 四处同步；1.1.0/1.2.0 不动） | §A-R1 = §B-1 |
| F2 | D8 溯源断言**白名单式**：「出处**字段行**含 http 链接 **或** commit 哈希实证格式 `[0-9a-f]{9,}`」+ 两卡出处行补可点 URL（Riverpod 卡裸 issue 号补链）；SP 卡「无公开出处实证卡过闸」= 白名单哈希分支收口 | §A-R2 = §B-2（§B 增量：URL 补链优先于纯放宽断言） |
| F3 | 配额基数 7 卡：「8→9」改「**7→8** ≤10 内」；附新漂移发现——CHANGELOG 0.6.4 自称「8/≤10」与现文实测 7 不符，**卡数无闸看守**，批二矩阵盘点时查计数错漏在哪步并挂闸候选 | §A-Y3 = §B-3 |

### 必修增量（单轮独有，§A 未覆盖）

| # | 消解项 | 来源 |
|---|---|---|
| F4 | D4 改法修正：**修订 frontend.md:38 加例外从句**（「只允许…单一强相关状态」后接「——纯 UI 局部状态（选中态/草稿/动画进度）本地组合除外；判定线 = 是否引入第二个底层数据源」），非仅 +bullet；否则 reviewer 照旧文 FAIL 豁免合规代码 | §B-8 |

### 应修吸收

| # | 消解项 | 来源 |
|---|---|---|
| F5 | 溯源声称降格：「Y11/Y12 已裁归 P3/P4」→「CHANGELOG 缓记口径」；外审 §7 裁决表除 P0 外空白如实写；附表「D1-D5↔N1-N5」与「D4↔Y5/D5↔Y9」两行矛盾修正（N3/N4→O3/O4）；批三「承 P2/P4」→ A7 实归 P3、A3/A4 已 0.6.3 部分收口口径更正；A5 补一行归置 | §B-5 |
| F6 | §5 影响面补三行：README:5 横幅「17 断言」字样同步义务（语义闸①不拦计数——矩阵盘点挂候选）；15K 口径段原地留全、MAINTENANCE 侧只收指针（防悬空+防第三份口径）；README:16 排查序改「memory/MAINTENANCE.md（消费工程记忆库规则）」消同名歧义 | §B-7 + §A-Y2（追加） |
| F7 | D2 补全：双静默分支都显式报告（plugin cache 非 git 仓分支先于 tag 缺失）+ tag 创建后 `git push origin v<ver>` + 消费者 `git fetch --tags` 入 MAINTENANCE 日历 | §A-Y2 + §B-10 |
| F8 | D3 注记与 distill.md:2 description「用户触发」同步改写（括注「或 AI 发起、闸内仍人工逐组裁决」）；frontmatter 行号 1-4 勘正 1-3 | §B-11 |
| F9 | §4.14 悬空引用修正（O1 行内即设计稿，删悬空节号） | §A-Y1 |
| F10 | D9 已知候选清单更正：删「§二 skill 冲突处理措辞差异」项（app/plugin :48 逐字相同），真不对称候选改列 :40/:44/:52/:56-65 | §B-6 |
| F11 | 版本口径裁决：agent 铁律修订记 patch（F4 消解后 D4 属措辞修订非新约定，留批一 1.0.2）；§5「D4 走 minor」表述删 | §B-4 |
| F12 | 批一验证措辞「矩阵行人审」改「批二盘点时补登」（矩阵 1.1.0 才存在的时序修正） | §B-3（维度结论） |
| F13 | release.sh 落点注记「usage sed 区间（2,7p）同步扩展」 | §B-9 |

### §A 勘误（主控对追加评审轮的复核）

| # | 勘误 |
|---|---|
| E1 | §A-R1「0.6.3 案底同款」→ 应为 **0.6.2 的 D1**（README 横幅滞后三版本、语义闸①首跑即抓案）；案底引用错版本，教训本体成立 |
| E2 | §A-R2 修法「出处**行**」定位不精确：Riverpod 卡无 URL 时白名单哈希分支也救不了裸 `flutter#73000` 文本——F2 的「补 URL」动作与白名单判据**两事都做** |
| E3 | §A-Y3 只改数字漏了衍生发现：CHANGELOG 0.6.4「8/≤10」与实测 7 的口径矛盾升格入 F3 |

### 背书共识（双轮独立确认，无需改动）

N1-N5 五件属实 · test-self 17/17 与 memory 8 模板基线属实 · layout.md:26 / checklist:31 / app:33 / README:127-130 等交叉引用无虚指 · D3/D5/D9 两步走/D15 不做清单方向正确 · **§3.5 两处官方文档外部断言经 §B 独立 curl 复核全部属实**（license 正式字段 / PreToolUse additionalContext 合法不阻断）——方案证据纪律整体经得住两轮对审，失实集中在「引用现状的时点数字」而非「机制设计」。
