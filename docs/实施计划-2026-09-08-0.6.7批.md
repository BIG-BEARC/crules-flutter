# 实施计划：0.6.7 批（i18n 残留收尾 + 语义闸补两条 + 5 分钟上手路径）

> 状态：已修订 v2（评审一轮：基线复核全过；1 🔴 白名单注记词 + 1 🟡 断言计数口径已消解，4 🟢 随批吸收——裁决记录见文末）· 依据：[外审-2026-09-08-0.6.2全面评审与社区对标](外审-2026-09-08-0.6.2全面评审与社区对标.md)（其 §2 五条 + P2 大半已随 0.6.3-0.6.6 并行批次消化，见 CHANGELOG；本批仅收存活欠账）；旧 0.6.3 计划两 🔴（现状脱节/版本占用）已作废删除
> 版本口径：**0.6.7（patch）**——残留修注 + 既有闸扩两条子检查 + README 增节，无模板交互环节变更（0.6.0 minor 先例判据）。
> 现状基线（2026-09-08 成稿前实测）：plugin.json **0.6.6**；test-self **PASS=15**；`grep '不推荐' agents/i18n.md`=0（i18n.md:14 残留在）；README 无上手路径节。
> Y11（守护覆盖矩阵）处置：**缓**——归 P3 与 A1 溯源闸同批（需先盘全仓断言面，不宜塞本小批）。

## 任务清单（单批三任务，无内部依赖）

| # | 任务 | 文件:行 | 改法 | 验收（可机检） |
|---|---|---|---|---|
| T1 | i18n easy_localization 裸并列残留 | `agents/i18n.md:14` | 「`flutter_localizations` + `intl` / `easy_localization` 等」→「`flutter_localizations` + `intl` / `slang` 等（easy_localization 维护缓慢不推荐——见 app 模板 §七 预设 A，与 `slang` 并列位一致）」 | `grep -c '不推荐' agents/i18n.md # =1` |
| T2 | 语义闸新增**两个独立 PASS 断言**（非 sem_ok 组内子检查——组内子检查不单独计数，:93 仅出一 PASS） | `scripts/test-self.sh`（紧随 :93 语义闸后新增） | ① `easy_localization` 注记断言（`el_note_ok`）：agents/ 命中行须带「不推荐**或**维护缓慢」注记（排除法沿用既有 `停更` 先例）；② `flutter_screenutil` 白名单断言（`su_wl_ok`）：agents/app/plugin/checklist 命中仅允许 app §七 两处，注记词**「已停更 或 维护缓慢」二选一命中**（:185 为「已停更（2024-05 起）」、:229 为「维护缓慢」——实测措辞不同，锚点用上下文关键词非行号） | `bash scripts/test-self.sh` PASS=15→**17**（有 dart 环境；无 dart 为 14→16，沿既有 SKIP 口径）；人为删注可复现炸（手验一次）；**README:5 横幅「语义闸 15 断言」同步改 17**（否则被闸①类问题反噬） |
| T3 | README 新增「5 分钟上手路径」节 | `README.md`「怎么用」节（:9）之前 | 四段各一句：装（install.sh 一条命令+三态说明指针）→ 日常触发（agents 按职责自动挂载 / skill 写 Flutter 代码时载 / hooks 静默守护）→ 收尾（pending-updates 队列 + Stop 提醒发生什么、怎么响应）→ 出问题看哪（`/help` → MAINTENANCE → CHANGELOG 排查序）。**各段末尾指向「场景地图」表/help 作权威源**——该表（README:30-39）已覆盖同信息密度，防第三份需人工同步的口径 | `grep -c '5 分钟上手' README.md # =1`；四关键词「安装/触发/收尾/排查」各 ≥1 |
| T4 | 收口：CHANGELOG 0.6.7 账本（含不采纳/缓记：Y11 缓至 P3）+ **历史计划散件归档**（git 未跟踪的 `docs/实施计划-2026-09-08-0.5.3批.md` 一并 git add 或显式裁决去留——账本只清一半即漂移）+ 双 json bump 0.6.7 + 全判据复跑 + test-self 人工必跑 | `CHANGELOG.md` + `.claude-plugin/*.json` | — | json version=0.6.7；test-self PASS=17（有 dart 环境）；`git status` docs/ 无未跟踪计划散件 |

## 风险与回退

- T2 ①排除词需与 T1 措辞对齐（「不推荐」或「维护缓慢」二选一写死在闸里），否则首跑即炸——对齐后先手验「删注复现炸」再收。
- T3 触及 README「与 crules 的关系」节附近，**与 P0（独立成库）有文件级交集**（非零交集）：串行执行本批先行，P0 后行时重对锚点即可。
- 本批与 0.6.6 无冲突（i18n.md 记忆库钩子行 :9 不动）。

## 评审裁决记录（v1 → v2）

| # | 评审项 | 裁决 |
|---|---|---|
| 🔴1 | screenutil 白名单注记词与 :229 实文不符（「维护缓慢」非「已停更」），闸首跑必炸 | 白名单注记词改「已停更 **或** 维护缓慢」二选一命中（不改模板措辞——动模板超本批最小改动口径） |
| 🟡2 | 「组内子检查」vs「PASS=17」计数矛盾（组内子检查不单独计 PASS） | 明确为**两个独立 PASS 断言**（`el_note_ok`/`su_wl_ok`）；README:5 横幅「15 断言」同步改 17 一并列入 T2 验收 |
| 🟢4 | 新节与「场景地图」表（README:30-39）双源漂移风险 | 新节各段末尾指向场景地图/help 作权威源（一句指针） |
| 🟢7 | 历史计划散件 `实施计划-2026-09-08-0.5.3批.md` 仍未跟踪 | T4 归档处置扩入：一并 add 或显式裁决去留 |
| 🟢8 | PASS=15 基线含本机 dart 前提（无 dart 为 14） | 验收补「有 dart 环境」限定，沿既有 SKIP 口径 |
