# 方案：1.0.0 评审后优化三批（N 组收口 + 守护体系化 + 内容补域）

> 状态：**v2 · 评审两轮已消解（F1-F13 全吸收，见 [评审-plan](评审-plan-评审后优化三批.md) §C）· 待需求方逐条裁 §9** · 日期：2026-09-08 · 关联需求：1.0.0 独立成库后用户「全面评审 + 优化方案」请求 · 面向对象：需求方裁决
> 依据链：本次全面评审（75 文件全量通读 + test-self 17/17 实跑 + 分发面实证）→ [外审 0.6.2](外审-2026-09-08-0.6.2全面评审与社区对标.md)（承其 Y/A/P 编号体系；**状态如实**：其 §7 裁决表除 P0 外空白，Y11/A1 的 P3 归属系 CHANGELOG 0.6.7「缓记」口径、非已裁——本方案是缓记池 + 评审新发现 N1-N5 的落地细化）
> 证据纪律：带【事实】的条目经 grep/实跑/官方文档核实；带【判断】的条目属评审观点。

---

## 2 核心决策（每条一句话可独立裁决）

| # | 决策 | 批次 |
|---|---|---|
| D1 | **LICENSE 定 MIT**（公库无许可证 = 法律模糊态，复用溢价值为零；Apache-2.0 与「暂不开源声明」为备选） | 批一 1.0.2 |
| D2 | **tag 断档修复走 release.sh 新增 `tag` 子命令** + check-imports 对无 tag 场景显式报告（现 memory 演进比对对所有 0.5.0+ 消费者从未生效，`2>/dev/null` 吞掉了静默失效） | 批一 1.0.2 |
| D3 | **distill frontmatter 注记「故意允许 AI 发起」而非补 disable**（交付汇报要求 AI 提示沉淀候选，能发起流程是特性；闸内仍逐组人工裁决） | 批一 1.0.2 |
| D4 | **聚合层铁律加豁免反例**：判定线是「是否引入第二个底层数据源」，纯 UI 局部状态（选中态/输入草稿/动画进度）不强制聚合层 | 批一 1.0.2 |
| D5 | **help.md 增「冲突优先级链表 + 最小概念五条」两节**；README 只回链不复制（防第三份孪生口径） | 批一 1.0.2 |
| D6 | **新建仓级根 MAINTENANCE.md**（维护者手册：改动 playbook + 例行义务日历 + 守护覆盖矩阵三合一，单文件承载防再开新文件） | 批二 1.1.0 |
| D7 | **守护覆盖矩阵上闸**：test-self 断言「分发面文件集 ⊆ 矩阵登记集」双向核——盲区从不可见变红；「无守护」也是矩阵合法行 | 批二 1.1.0 |
| D8 | **溯源闸分两层**：结构断言入 test-self（每坑卡必含`最后核验：`+出处含 http 链接）；月度 revalidate workflow（只报告不红）缓裁 | 批二 1.1.0 |
| D9 | **孪生内容级等值闸两步走**：先人肉 diff 分类「合法不对称 vs 真漂移」出豁免清单，再对豁免后内容上 `diff` 等值断言 | 批二 1.1.0 |
| D10 | **新增 `references/testing.md`**（widget/integration/golden 三件套）；golden 首个落点 = **checklist 9 金额展示铁律的视觉回归锁**（prose 变机检） | 批三 1.2.0 |
| D11 | **state restoration 能力边界卡**入 skill 坑库（RestorationMixin 边界 + 订单草稿持久化推荐 + 与 SP 白屏坑卡联动裁决） | 批三 1.2.0 |
| D12 | **performance.md 补 bundle size / RepaintBoundary 两小节**（字体打包与包体张力如实写权衡、不给禁令） | 批三 1.2.0 |
| D13 | **A4 监控栈坑走 distill 采集任务**（素材在 saas-cashier 生产仓，需需求方提供 commit/复盘输入，本包侧只留框架） | 批三 1.2.0 |
| D14 | **memory-guard hook 出设计草稿入观察池**——PreToolUse `additionalContext` 已核实官方支持且不阻断（本轮查证收口），但按 Y11 裁决精神与批二同批才值 | 池 |
| D15 | **明确不做**：双模板单源化 / Windows 深度适配 / 跨工具适配（X2 维持）/ docs 瘦身运动 | 不裁即生效 |

---

## 3 现状

- 1.0.0 评审结论：最强项 = 核验纪律/溯源链/自我审计文化；最弱项同源 = **守了结构没守语义、守护是点状非体系**（[外审 0.6.2](外审-2026-09-08-0.6.2全面评审与社区对标.md) Y11/Y12 已点名并裁归 P3/P4，本方案即其细化执行）。
- 评审新发现五件（N1-N5，全部【事实】级核实）：

| # | 位置 | 问题 | 本方案处置 |
|---|---|---|---|
| N1 | 仓根 | 公库（remote=github.com/BIG-BEARC/crules-flutter.git）无 LICENSE 文件 | D1 |
| N2 | [check-imports.sh:22-26](../scripts/check-imports.sh#L22-L26) | `git diff v$STAMP HEAD` 依赖 tag，全仓仅 v0.4.0 一个 tag——0.5.0+ 装的戳全部静默不生效（`2>/dev/null` 吞） | D2 |
| N3 | [hooks.json:9,21,32](../hooks/hooks.json#L9) | 固定 `python3`，Windows 有 `python`/`py` 也全灭 | 观察池 O3 |
| N4 | [ci.yml:39](../.github/workflows/ci.yml#L39) | 链接检查只扫 `skills/**/references/*.md` | 观察池 O4 |
| N5 | [distill.md:1-4](../commands/distill.md#L1) | 无 `disable-model-invocation` 而 init/update-memory 有，意图未记载 | D3 |

- Y5（聚合层无豁免口）/ Y9 残余（冲突优先级链用户不可感知）：外审已认定、本方案 D4/D5 收口。
- 内容盲区承 0.6.2 外审 A2/A4/A7（[外审 §5](外审-2026-09-08-0.6.2全面评审与社区对标.md)），D10-D13 落地。

## 3.5 行业参考

| 参考对象 | 关键做法 | 对本方案启示 | 出处 |
|---|---|---|---|
| Claude Code 官方 hooks 文档 | `additionalContext` 支持 PreToolUse，注入 tool result 旁、不返回 permissionDecision 即不阻断；文案须事实陈述（祈使系统指令口吻触发注入防御）；resume 重放存 transcript——注入内容不放时间戳类易变值 | D14 设计草稿的三约束直接照此定（memory-guard 文案 = 事实句 / 无常变值 / 不 deny） | [官方 hooks 文档](https://code.claude.com/docs/en/hooks)（本轮 subagent 抓取原文核实） |
| Claude Code plugin manifest 官方 schema | `license` 为 schema 内正式字段；未知字段运行时静默忽略、`plugin validate` 记 warning | D1 落法 = LICENSE 文件 + plugin.json `license` 字段（schema 合规，非 hack） | [plugins-reference](https://code.claude.com/docs/en/plugins-reference)（同上核实） |

## 4 目标方案（逐条：落点 → 改法 → 验证）

### 批一 · 速赢（1.0.2，约半天，全在本仓、无结构风险）

> **v2 版本注记（F1）**：原标 1.0.1 已随 `59832d8`（docs 清账）发布——撞号则消费者 cache 按版本刷新纪律永不更新（release.sh 头注），全局改判 1.0.2。定稿前跑 git log + 双 json（0.6.2 D1 案底教训）。

**4.1 D1 LICENSE**
- 落点：新建 `LICENSE`（MIT 全文）；[plugin.json](../.claude-plugin/plugin.json) +`"license": "MIT"`；README「怎么用」节 +1 行。
- 备选：Apache-2.0（在意专利授权时）；「保留所有权利」README 声明（暂不开源时——现状最差是模糊，声明即收口）。
- 验证：`claude plugin validate` 零 warning。

**4.2 D2 tag 修复**
- 落点：[release.sh](../scripts/release.sh) case 分支 +`tag)`：校验 `HEAD` 的 plugin.json 版本 == `<ver>` 后 `git tag v<ver>` **并 `git push origin v<ver>`**（防 tag 打在 bump 前空态；不推则消费者 clone 永不可见——F7）；头注用法行同步，**注意 usage() 系 `sed -n '2,7p'` 硬编码行号，新增用法行使区间外溢——sed 区间同步扩展**（F13）；[check-imports.sh](../scripts/check-imports.sh) 演进比对**两个静默分支都显式报告**（F7）：① SRC 非 git 仓（真实消费者装的是 plugin cache——此分支先于 tag 判定被走到，报「SRC 非 git 仓，演进比对需克隆路径」）② `git tag -l "v$STAMP"` 空（报「源仓无该 tag，比对不可用」），替代现 `2>/dev/null` 静默。
- 回补 v0.5.0-v1.0.0 历史 tag：可选（CHANGELOG 锚点 commit 定位），不回补不影响今后——**建议不回补**（历史锚点模糊，回补错 tag 比无 tag 更坏【判断】）。
- 消费者侧配套：`git fetch --tags` 指引入根 MAINTENANCE.md 日历（批二 D6 承载，F7）。
- 验证：test-self +1 断言（`release.sh tag 9.9.9` 与 HEAD 版本不符应报错，exit≠0）。

**4.3 D3 distill 注记**
- 落点：[distill.md](../commands/distill.md) frontmatter 下注记 + **同步改写 description 括注消共存矛盾**（F8）：现 description:2 写「用户触发，支持 --scope」与新注记「故意允许 AI 发起」打架——description 改为「**用户触发或 AI 发起**（交付汇报沉淀候选提示依赖此），支持 --scope；**闸内裁决仍是人工逐组**」；frontmatter 实为 1-3 行（v1 写 1-4 系行号微偏）。
- 验证：人读即证，无需闸（批二矩阵盘点时补登，F12）。

**4.4 D4 聚合层豁免**
- 落点：**修订 [frontend.md:38](../agents/frontend.md#L38) 既有铁律句加例外从句**（非 +1 bullet——v1 的 +bullet 会与「**只允许** watch…单一强相关状态」同节并存成表面矛盾，reviewer 照旧文仍 FAIL 豁免合规代码，F4）：句后接「——**例外**：选中态 / 输入草稿 / 动画进度等纯 UI 局部状态可直接本地组合，不强制聚合层；判定线 = 是否引入第二个**底层数据源**」；[backend.md:38-39](../agents/backend.md#L38) 同款从句对称改。checklist 1 已写「跨源组合」无需动。
- 验证：双文件措辞对称（批二矩阵盘点时补登，F12）。版本口径：铁律措辞修订记 **patch**（随批一 1.0.2 走；v1「D4 走 minor」表述删，F11）。

**4.5 D5 help.md 两节**
- 落点：[help.md](../commands/help.md) 全景节后 +「冲突时听谁的」表：`需求方当前指令 > 项目根 CLAUDE.md（模板）> superpowers / dart-flutter skills > flutter-rules skill > 模型默认`；注脚两行——「deny-list hook 不在链内（硬闸独立生效无放行）」、「AO lint 独立生效（静态层不经判断）」。+「最小概念五条」节（双 Gate / 证据5级 / 只报不改双审 / memory 永不覆盖+distill 闸 / 5 分钟路径回链 README）。
- README 侧**零改动**（help 为权威全表、README 已有指针——避免第三份口径）。**注**：新增两节不得含「hooks ×N」样式字面串——语义闸②取 `grep -oE 'hooks ×[0-9]+' | head -1`，前插会改口径（F6 衍生自查）。
- 验证：语义闸②不受影响（hooks 数口径不变）。

### 批二 · 守护体系化（1.1.0，主题「让守护自身可审计」，即已裁 P3 的扩充）

**4.6 D6+D7 维护者手册 + 覆盖矩阵（一件工程两产物）**
- 落点：新建根 `MAINTENANCE.md`（头注与 [memory/MAINTENANCE.md](../memory/MAINTENANCE.md) 互指声明「同名不同物：前者=本库维护者手册，后者=消费工程记忆库运行时规则」；**README:16 排查序指针同步改「memory/MAINTENANCE.md（消费工程记忆库规则）」消歧义**，F6③）。三节：
  1. **改动 playbook**（表）：改 hook → fixture+py_compile；改模板 → 孪生对照改 + D9 内容闸；改 skill references → CI 链接步 + D8 结构闸；改 README/help → 语义闸①②；bump 流程（release.sh 头注收敛 + tag 推送步骤）；外审节奏。
  2. **例行义务日历**：坑节核验 / 栈审视 / `/context` 复测三项从 [README.md:127-130](../README.md#L127-L130) 收敛，各带「上次执行」栏；**15K 线口径段原地留全于 README、MAINTENANCE 侧只收指针**（防横幅 :5「归因口径见维护节」悬空 + 防第三份口径，F6②）；`git fetch --tags` 消费者指引入本日历（F7）。
  3. **守护覆盖矩阵**：分发面文件 × 守护断言映射，先盘点生成初版（预期照出 2-3 盲区，「无守护」显式行；**盘点项含「断言计数入横幅」候选闸**——README:5 横幅「17 断言」字样 17→21 后将重演 0.6.2 D1 同型漂移且语义闸①只比版本号不拦，F6①；**含 CHANGELOG 0.6.4「8/≤10」与实测 7 卡口径矛盾的计数溯源**，F3）。
- README 维护节相应行改指针（防双份）。
- 验证：test-self +1 断言（分发面文件集 ⊆ 矩阵登记集，双向：矩阵列不存在的文件也红）。
- 成本诚实账：矩阵初版盘点 1-2h 是人肉活，断言本身 ~10 行。

**4.7 D8 溯源闸（v2：判据白名单化 + 两卡补链——评审 F2 消解）**
- 结构断言（进 test-self）：[platform-pitfalls.md](../skills/flutter-rules/references/platform-pitfalls.md) 每张 `### [` 卡节的**出处字段行**（锚 `- 出处：` 行，勿锚节序）必含 **http 链接 或 commit 哈希实证格式 `[0-9a-f]{9,}`**——实证卡无公开外链是合法形态（SP 卡私有仓 commit），纯放宽成「有出处字样」则闸失去可点开价值。v1 判据（仅 http）装闸即红：Riverpod 卡 :25 裸 `flutter#73000` 文本、SP 卡 :33 哈希引用两张当场 FAIL——**同批先补链再装闸**：Riverpod 卡 issue 号补 URL（行内已有裸号，一行成本）；SP 卡哈希引用即白名单合法分支，不补链。
- 月度 workflow（`.github/workflows/revalidate.yml`，链接检查 + 核验日 >90 天清单，只报告不红）：**与「治理从简」哲学的张力如实呈报——它是把已承诺义务从「记得」变「被提醒」，不新增义务；但 CI 面扩大，建议本批只做结构断言、workflow 单裁**（Q3）。

**4.8 D9 孪生内容等值闸（两步走）**
- 步骤①发现：`awk` 提取双模板 §一/三/四/五/六 逐条 diff，逐条分类出豁免清单。已知候选经评审修正为 **3 真 1 撤**（v1 第 4 项不成立：「§二 skill 冲突处理措辞差异」——两文件 :48 该行逐字相同；真 §二不对称改列 :40 唯一硬冲突注 / :44 触发词覆写例 / :52 commit 泛描述禁例 / :56-65 破坏性操作展开，F10）：app:33 硬编码条多「用户可见字符串」✅、plugin:49 发版特殊性行 ✅、app:87 设计/编码默认句 ✅。
- 步骤②上闸：豁免后内容 `diff` 归一化须空——「同源对照改」从人肉纪律变机检。
- 风险注记：豁免清单本身是新的维护点（矩阵行登记）；步骤①裁决归需求方（合法不对称的判据是语义判断非机判）。

### 批三 · 内容补域（1.2.0，承 0.6.2 外审吸收清单；**A7 外审实归 P3、A3/A4 已由 0.6.3 部分收口——v1「承 P2/P4 已立项未落」表述过头，如实降格，F5**）

**4.9 D10 testing.md**
- 落点：新建 [references/testing.md](../skills/flutter-rules/references/testing.md)：widget test（WidgetTester pump/tap/enterText/drag 四动作 + `findsOneWidget` 断言 + **ValueKey 定位**——与 [layout.md](../skills/flutter-rules/references/layout.md) 列表 key 节闭环，layout.md:26「widget 测试锁死」的落点在此）/ integration test（关键路径选型、不进 CI 每 PR 的成本注记）/ golden test（`matchesGoldenFile`、本地 `--update-goldens` 重生成 + 人审 diff、CI mismatch 红、**字体环境差异坑**：CI/本地字体不一致须固定打包字体，与字体策略节联动）。SKILL.md 取用表 +1 行。
- 首个落点提案：收银键盘 / 购物车行模板 / 小票排版 / **金额展示铁律视觉回归**（checklist 9「主金额固有宽完整显示不可截断」prose → golden 机检——本方案里机制化跃迁价值最高的一条【判断】）。
- 验证：链接检查自动覆盖（新文件在 references glob 内）。**A5 归置**（外审「与 A2 合并立项」项：coverage 收集 / mock 生成 / runtime 错误修复流程卡）：**归 testing.md 一节指针即可**——三者皆 dart-flutter skills 已有执行面（`dart-collect-coverage` / `dart-generate-test-mocks` / `dart-fix-runtime-errors`，app 模板 §十一速查表已挂），本库不重抄操作步骤（SKILL.md 分工纪律），testing.md 只在「测试选型」表给三行「何时用哪个 skill」指针（F5 补）。

**4.10 D11 state restoration 卡**
- 落点：[platform-pitfalls.md](../skills/flutter-rules/references/platform-pitfalls.md) Flutter SDK 节 +1 卡（配额 **7→8 ≤10 内**——v1 写 8→9 系基数错，现文实测 `grep -c '^### \['` = 7，F3；CHANGELOG 0.6.4 自称「8/≤10」与实测 7 的口径矛盾随批二矩阵盘点溯源，F3）：归属 Flutter SDK；`restorationScopeId`/`RestorationMixin` 能力边界（系统杀进程恢复有效、不覆盖冷启动完整订单态、区间如实标）；推荐形态 = 订单草稿持久化（保存时机/启动恢复/幂等）+ **与 SP 白屏卡联动**（init 失败降级时草稿丢失语义须显式裁决——降级默认配置的代价要写明）。
- checklist +1 条件条：「涉订单/交易态：被杀恢复策略已裁决（系统 restoration / 草稿持久化 / 明确放弃并记 decisions/）」。

**4.11 D12 performance.md 两节**
- bundle size：`flutter build --analyze-size` / DevTools 剖析 / 资产审计；**字体全字重打包与包体的张力给权衡句不给禁令**。
- RepaintBoundary：加的条件（复杂静态子树高频重绘、DevTools 重绘水波观察）+ 默认不加（多一层 layer 非免费）。

**4.12 D13 A4 采集任务**
- 本包侧动作 = 零（框架即 [error.md](../agents/error.md) 已有职责行）；素材采集走 `/distill --scope 监控栈`，输入 = 需求方提供 saas-cashier Bugly 符号化 / 崩溃分拣 / 日志轮转相关 commit 或复盘。**此项不阻塞批三合批**。

### 观察池（有设计、不动）

- **O1 memory-guard hook**（D14）：设计草稿已完备（PreToolUse `additionalContext` 官方支持已核实——此前「未查证」收口，§B 轮 curl 原文二次复核属实）：guard-map 从 INVARIANTS「关联」字段派生 + 路径前缀匹配 + 会话去重 + 事实陈述文案（官方：祈使系统指令口吻触发注入防御）+ 零易变值（官方：resume 重放存 transcript，不放时间戳/SHA 类）；落点须走 distill（消费项目 `.claude/settings.json`）。**注**：v1「设计稿即本方案 §4.14 存档」系悬空引用（正文止于 §4.12，F9）——修正为：**O1 本行即设计稿存档**，五要素齐（触发/机制/文案/去重/落点）。**建议**：触发条件 = 真实工程出现一次「没读 INVARIANTS 就改了关联文件」实证。
- O2 custom_lint 二期（inline 颜色 / ref 传参 / import 分组）——独立立项。
- O3 hooks.json `python3 || python` 回退（N3，README 已声明降级，收益低）。
- O4 CI 链接检查扩 README/docs（N4，网络抖动面变大）。
- O5 团队 onboarding 文档（有第二人再付）。

## 5 影响面

| 面 | 影响 |
|---|---|
| 消费者（已装工程） | 批一批二**零模板变化**（改动全走 plugin 通道 + 仓内脚本）；批三 skill references 新域文件走 plugin 通道自动，模板/checklist 仅 +1 条件条（批三合并 `.new` 时注意） |
| 常驻 token | +testing.md 不占常驻（按需 Read）；help.md +2 节只增触发载体积；**无新增 @import** |
| 分发版本 | **1.0.2**（v1 误标 1.0.1——已随 `59832d8` 发布，撞号即 cache 不刷新，F1）/ 1.1.0 / 1.2.0 三段（D3 属交互语义零变化走 patch；D4 经 F4 改判为措辞修订走 patch 留批一，v1「minor」表述删，F11；批三新内容域走 minor） |
| test-self | 17 → 最多 21 断言（+tag 报错 / 矩阵双向 / 溯源结构 / 内容等值）——每条均为本方案新增而非既有变红（**溯源断言以 F2 白名单判据 + 两卡先补链为前提**，v1「出处含 http」措辞装闸即红已消解） |
| 对外口径 | README:5 横幅「17 断言」字样随新增断言**逐批同步**（0.6.2 D1 同型坑，语义闸①不拦计数——批二矩阵盘点挂候选闸，F6①）；README:5「当前状态」横幅版本号仍由语义闸①看守不变 |
| 同名/指针 | 根 MAINTENANCE.md 与 memory/MAINTENANCE.md 头注互指 + README:16 排查序指针改标「消费工程记忆库规则」消歧义（F6③）；15K 口径段留 README 原地、MAINTENANCE 只收指针（F6②） |
| 孪生 | README 维护节改指针（D6）；help/README 同源口径不扩面（D5 README 零动） |
| CI | workflow 若单裁通过 +1 文件（月度、不红）；不裁则零 |

## 6 测试与验收

### 6.1 验收标准

| # | 判据 |
|---|---|
| 1 | 全批落地后 test-self 全绿（断言数 == 17+新增数，FAIL=0） |
| 2 | `claude plugin validate`（或安装态 `claude plugin details`）零 warning，`license` 字段被识别 |
| 3 | N1-N5 五件逐条可 grep 复验（LICENSE 在 / check-imports **两分支**均有显式报告 / distill description 改写生效且无「用户触发」裸矛盾 / **frontend:38 例外从句在位** / help 两节在） |
| 4 | 覆盖矩阵初版：分发面每个 `.md`/`.py`/`.sh` 有行；「无守护」行 ≤ 初版盘点数且逐条注明裁决去向 |

### 6.2 自测用例表（执行期挂证据分级）

| 用例 | 前置/步骤 | 预期 | 证据级 |
|---|---|---|---|
| tag 子命令报错 | `bash scripts/release.sh tag 9.9.9`（HEAD 版本≠9.9.9） | exit≠0 且提示版本不符 | 测试环境 |
| tag 正常路径 | 临时 bump 后跑（沙箱仓） | tag 创建、再跑幂等报错（已存在） | 测试环境 |
| check-imports 无 tag 报告 | 对装戳 v0.6.0 的项目跑（源=本地克隆、无该 tag） | 显式「源仓无该 tag，比对不可用」行，非静默 | 测试环境 |
| check-imports 非 git 仓报告 | 源指向 plugin cache（非 git 仓）跑 | 显式「SRC 非 git 仓，演进比对需克隆路径」行（F7 分支①） | 测试环境 |
| tag 推送语义 | 沙箱仓 + 本地 bare remote 跑 tag 子命令 | tag 创建且推至 remote（不推则消费者永不可见，F7） | 测试环境 |
| 矩阵双向断言 | 临时在矩阵删一行 | test-self 该断言 FAIL；恢复后 PASS | 测试环境 |
| 溯源结构断言 | 临时删某卡出处行的 URL/哈希 | 该断言 FAIL；恢复后 PASS（**白名单判据：两卡先补链后装闸，装闸当轮即绿**，F2） | 测试环境 |
| 内容等值闸 | 步骤①产出的豁免清单外改一字 | FAIL（装闸即抓） | 测试环境 |
| 消费态三场景 | flutter create 壳真跑 install/check-imports | 与 0.2.2 首验同绿 | 测试环境 |
| details 验证 | `claude plugin details crules-flutter@crules-flutter-market` | Skills 行数含 testing.md；license 显示 | 目标环境 |

## 7 分阶段落地

批一（1.0.2）→ 批二（1.1.0，D9 步骤①需需求方裁决豁免清单，是唯一人肉卡点）→ 批三（1.2.0，D13 素材依赖另线不阻塞）。每批独立可验收、独立 bump——**不混批原则沿用 1.0.0 先例**（结构决策与内容增补不同批）。**批二装溯源闸前须先完成两卡补链（F2 前置）**：Riverpod 卡 :25 补 URL 属批二首步，不占批一。

## 8 风险与回退

| 风险 | 缓解 | 回退 |
|---|---|---|
| D9 豁免清单判错，把真漂移当合法不对称永久放行 | 判据逐条落审查记录（`docs/评审-plan-` 惯例）；矩阵登记「不对称」行 | 单条翻案即改豁免集，断言不翻车 |
| D7 矩阵初版盘点后「无守护」行过多致治理压力观感 | 「无守护」行标注裁决去向即可，**矩阵是可见性不是全守承诺** | 无 |
| D1 MIT 裁决后想改（如转闭源） | LICENSE 文件 + 一行 json，revert 干净 | git revert |
| 月度 workflow 网络抖动误报 | 只报告不红已消解阻断面 | 删 workflow 单文件 |
| 断言从 17→21 后误拦日常改动的维护税 | 每条新断言带「为什么」注释（test-self 既有文风）；语义闸首跑抓自伤先例（0.6.2）已在文化内；**横幅「17 断言」计数同步入 §5 对外口径行（F6①）防 D1 同型漂移重演** | 单条撤闸留痕 CHANGELOG |
| D8 白名单哈希分支被滥用（有公开 issue 却偷懒只放哈希过闸） | F2 双动作：判据白名单 + 两卡补链——白名单是**实证卡形态豁免**不是**偷懒豁免**；矩阵行登记判据原文 | 收紧成「哈希分支仅限出处行标注『实证』前缀」 |

## 9 待确认事项

| # | 问题 | 责任 | 阻塞？ |
|---|---|---|---|
| Q1 | LICENSE 选型：MIT / Apache-2.0 / 暂不开源声明？ | 需求方 | 阻塞 4.1 |
| Q2 | 历史 tag 回补：建议不回补，同意？ | 需求方 | 不阻塞 |
| Q3 | D8 月度 workflow 与「治理从简」张力：本批做 or 缓？ | 需求方 | 不阻塞（结构断言先行） |
| Q4 | D9 步骤①豁免清单裁决（合法不对称判据）何时安排 | 需求方 | 阻塞 4.8② |
| Q5 | D13 监控栈素材提供时间点 | 需求方 | 不阻塞（批三可拆） |
| Q6 | O1 memory-guard 的触发实证标准是否认可（「一次没读就改」即立项） | 需求方 | 不阻塞 |

---

## 附一：与 0.6.2 外审编号映射（溯源 · v2 按 F5 如实降格）

> 状态口径先说清：**外审 §7 裁决表除 P0（独立成库）外全部空白**——下表「P3/P2」非「已裁」，实际依据 = CHANGELOG 0.6.7「缓记：Y11 守护覆盖矩阵归 P3（与 A1 溯源闸同批）」一条 + 0.6.2 留池叙述；Y12/A2/A7 无落池记录，系本方案自主归档。

| 本方案 | 外审编号 | 状态变化（如实） |
|---|---|---|
| D6/D7 | Y11/Y12 | Y11 有 0.6.7 缓记 P3 口径、Y12 无落池记录——本方案落细化 |
| D8 | A1/Y6 | 同批缓记口径（CHANGELOG 0.6.7 与 D7 同捆） |
| D9 | Y1 残余 | 外审必修部分 0.6.2 已收口 → 内容级补闸 |
| D10/D11/D12 | A2/A7/A3 | 外审 §6 建议 P2/P3（A7 在 :110 归「与 P2 可并」的 P3 批）——**A3 性能已由 0.6.3 落地大半**，本方案仅补 bundle size/RepaintBoundary 残角 |
| D13 | A4 | 外审标「内部待补」（素材本团队侧）——非社区吸收项，本方案归 distill 采集任务 |
| — | **A5（coverage/mock/runtime）** | **归 testing.md「测试选型」三行指针不重抄**（dart-flutter skills 已有执行面，§4.9 验证行，F5） |
| — | **X1-X3** | 维持不吸收（外审已排除，无翻案） |
| D1/D2/D5 | N1/N2/N5' | **本方案新发现**（1.0.0 评审产出，外审未覆盖；N5' = D5 系 Y9 补口非 N 组） |
| D3 | N5 | 新发现（frontmatter 一致性） |
| D4 | Y5 | 外审 🟡 → 本方案收口（v2 改法见 F4） |

---

## 评审记录（2026-09-08，主会话评审轮 · 判据全部实跑/grep 复核）

> **v2 处置总览**：本两轮评审（本段 §A + [评审-plan §B](评审-plan-评审后优化三批.md) 独立上下文轮，2🔴 双轮同判）合并为 **F1-F13 消解清单**（评审-plan §C），**v2 已全量吸收**——F1 版本 1.0.2 ✅ / F2 断言白名单+两卡补链 ✅ / F3 配额 7→8 ✅ / F4 D4 改「修订 :38 从句」✅ / F5 溯源声称降格+A5 归置 ✅ / F6 影响面三漏项 ✅ / F7 D2 双分支+push/fetch ✅ / F8 distill description 同步改写 ✅ / F9 §4.14 悬空修正 ✅ / F10 D9 候选撤一 ✅ / F11 版本口径统一 patch ✅ / F12 时序措辞 ✅ / F13 usage sed 区间注记 ✅。本节原文按史实保留不改写；本段「0.6.3 案底」系引用错版本（应 0.6.2 D1），勘误见评审-plan §C-E1。

> 总判：**骨架成熟（分批/不裁清单/成本诚实账/回退表齐），但 2 🔴 裁决前必修**——其中 R1 是 0.6.3「基线过期」同款错误模式，R2 是 0.6.7 评审抓到的「白名单判据 vs 实文措辞」首跑即红同款。修后可裁。

### 🔴 必修（2）

| # | 发现 | 证据 | 修法 |
|---|---|---|---|
| R1 | **批一版本号已被占用——方案写在对过期基线上**。方案标注批一 `1.0.1`，但 1.0.1 已随 `59832d8`（docs 清账）发布：双 json / CHANGELOG / README banner 四处一致 1.0.1 | `grep version .claude-plugin/*.json` → 1.0.1；git log 实证 | 批一改 **1.0.2**，§2 D1-D5 批次列 / §4.1 题注 / §5 分发版本行 / §7 四处同步改。**教训重申：定稿前跑 git log + 双 json——0.6.3 案底同款** |
| R2 | **D8 溯源闸首跑即红**（同 0.6.7 评审 🟡 型：硬编码判据 vs 实文）。断言「出处含 http 链接」但现存 7 卡中 2 张不含：`platform-pitfalls.md:25`（Riverpod 卡 `flutter#73000 + riverpod discussion #3043` 纯文本无链接）、`:33`（SP 卡 `实证 saas-cashier af24988e3/4017e21c1/…` commit 哈希——**私有仓不可能有链接**） | `grep -n '出处' platform-pitfalls.md` 全量核对 | 判据改白名单式：「出处行含 http **或** commit 哈希实证引用格式（`[0-9a-f]{9,}`）」——实证卡无外链是合法形态 |

### 🟡 建议修（3）

| # | 发现 | 修法 |
|---|---|---|
| Y1 | O1「设计稿即本方案 §4.14 存档」悬空——正文只到 §4.12，无 4.14 | 补 §4.13 存档小节，或改为「O1 行内即设计稿」注记（五要素其实已在 O1 行内完备） |
| Y2 | D2 效果表述过强：`git diff v$STAMP` 有**两个**静默分支——① SRC 非 git 仓（真实消费者装的是 plugin cache，走此分支，**先于 tag 缺失被跳过**）② 无该 tag（本地克隆）。方案只救② | ①也显式报告（「SRC 非 git 仓，演进比对需克隆路径」），或文案如实限定效果仅覆盖克隆路径消费者 |
| Y3 | D11 配额计数错：「配额 8→9 ≤10 内」——现 **7** 张卡（`grep -c '^### \['` 实测），加一为 8 | 改「7→8 ≤10 内」；此类数字入验收标准，笔误即裁决误差 |

### 🟢 核实通过（背书）

- **N1-N5 五件【事实】全部为真**：无 LICENSE ✓；全仓仅 `v0.4.0` 一 tag + `2>/dev/null` 吞 ✓；hooks.json 固定 `python3` 三处 ✓；CI 链接步 glob `references/*.md`（ci.yml:39 行号精确）✓；distill 无 `disable-model-invocation` 而 init/update-memory 皆有 ✓
- 交叉引用无虚指：layout.md:26「widget 测试锁死」✓；checklist 9 金额展示铁律原文在 :31 ✓；D9 已知候选 app:33「用户可见字符串」（plugin:33 无此词）✓；test-self 17 断言基线实跑全绿 ✓；README:127-130 例行义务三条确可收敛入维护者手册 ✓
- 判断面背书：D3 注记优于补 disable（能发起是特性）/ D5 README 零动防第三份孪生 / D9 两步走且不对称裁决归人（机判不可为，判据是语义）/ D15 明确不做清单 / §8 成本与回退表——方案最有价值的部分，方向正确。

### 裁决建议

1. 修 R1 + R2（版本 1.0.2 / 断言白名单）后逐条裁 §9 Q1-Q6；
2. **Q1 建议 MIT**：定位公库规则资产、复用溢价值最大、revert 干净（§8 已注）；
3. 批一修完即快执（无实质风险），批二 D9 步骤①留需求方裁决窗口，批三 D13 另线不阻塞。
