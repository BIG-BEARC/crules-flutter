# 升级合并体验优化 — 方案 v2（终态 D 已裁，2026-09-24）

> 状态：**已裁待实施**。裁决链：2026-09-23 实操暴露 `.new` 全人工痛点 → v1 呈甲/乙两案 → plan-reviewer 隔离评审（docs/评审-plan-升级合并体验优化.md，10 发现：乙因 §七 中部覆盖缺口否决）→ 需求方 09-24 裁「甲立批（带自存 base）+ 终态丙」→ 主控实测解锁 D（绝对路径 @ 导入 `-p` 模式实证，见 §3）→ 补充枚举设计空间（B'/D2/E/F/G 全否，见 §3.4）→ 需求方再裁「终态改 D」。
> 本稿所有数字/路径/行号出自会话实际运行输出（红线：引用皆有实测依据）；实施时以当时文件重核。

## 0 问题陈述（实测，口径=净改动行 `diff | grep -c '^[<>]'`）

`install.sh --upgrade` 把已存在文件的新版全文落 `xxx.new` 伴生，合并全人工。两处摩擦：

1. **差异不可见**：`.new` 未跟踪（`git diff` 空面）+ 现状文件未被改动（源代码管理面板无红）——需求方在工程里「没看到差异」，实际净改动 saas-suite 128 行 / light 250 行。
2. **人工量与决策量不成比例**：逐行对照几百行，真人只需拍板冲突块——实测两工程**各仅 1 处且同形**（项目尾部内容撞戳位/gen 块），其余 8 伴生用户零改动（对 base diff=0）。

## 1 三向合并可行性证据（2026-09-23 试跑）

base=安装版本模板（cache 旧版目录，当日 17 目录俱在、含 1.0.6/1.0.25——评审 #2「已失效」经主控两次 `ls` 实拍推翻；但 cache 保留策略宿主不可控，见 §2 base 自存）、ours=现状、theirs=`.new`：

| 工程 | 净改动行 | 冲突块 | 伴生处置 |
|---|---|---|---|
| saas-suite（1.0.25→1.0.40） | 128 | 1（§十三撞戳位） | 7 篇零改动直替 + AO 伴生逐字节同删除 |
| light（1.0.6→1.0.40，跨 34 版） | 250 | 1（项目附录撞档位预设块+戳位） | 同上 |

两处冲突解法同构可程序化（ours 全保留、去旧戳、their 新块接后），本轮已脚本化落盘两工程（合并后 Stop 闸陈旧提醒消失，实测）。

## 2 方案甲 — `--upgrade` 内置三向合并（迁移机器 + 越界守卫，本批实施）

改动面：`scripts/install.sh` + `commands/init.md`（`.new` 叙事随批改写，评审 #8）+ install.sh 尾注「下一步④」+ test-self 断言 + fixture（mktemp 唯一目录，评审 #9）。

- **base 自存（D-5 已裁）**：直替/合并成功时把当轮模板全文自存 `CLAUDE.md.crules-base`（伴生快照，gitignore 列入）——下轮 base 恒可得，斩断 cache 存续依赖；cache 旧版仅作历史工程首轮的过渡 base，缺失退 `.new` 人工并明说。
- 合并步默认开（`--new-only` 留退路，D-2）：对 base 零改动 → 直替；有改动 → `git merge-file --diff3`；零冲突写回并刷戳，有冲突写回合并结果 + `xxx.conflicts` 摘要（块号+两侧预览），控制台列待人工块数。
- **三条护栏（评审 #6）**：写回前强制 `.bak`；冲突未解**禁止**更新戳（否则 check-imports.sh:15 假绿）；`.conflicts` 未清零时下一步文案含「宪法文件当前含冲突标记」警示。
- 降级守卫（1.0.34）、memory 永不覆盖、AO 伴生语义均不变。
- 边界如实：① `git merge-file` 只依赖 git **二进制**，不依赖目标/cache 是 git 仓（评审 #7 措辞修正；缺 git 退 `.new`）；② 同段语义重写宁冲突勿错并；③ 冲突自动解只认「尾部内容撞戳位/gen 块」可枚举形态，其余呈人。
- 验证设计（先红后绿）：fixture 四态（零改动直替/自动并成功/冲突带标记/base 缺失退路）+ 真树负控 + mktemp。

## 3 方案丁（D）— 指针引用终态（2026-09-24 裁）

### 3.1 形态

消费工程 `CLAUDE.md` 缩为 ~3 行：

```
<!-- crules-flutter: v1.0.40 @ 2026-09-23 -->
@/Users/<user>/.claude/plugins/cache/crules-flutter-market/crules-flutter/1.0.40/app/CLAUDE.md
@CLAUDE.project.md
```

- 宪法正文每机只活一份（plugin cache），**拷贝漂移结构性不存在**；`CLAUDE.project.md` 项目面（§七定稿、项目速查、附录、协作偏好）安装器永不触碰（同 memory/ 待遇）。
- 升级 = `claude plugin update` + install.sh 重写 2 行（戳+指针）。陈旧闸**不变**且自洽：plugin update 后未跑 install.sh → 戳旧 → 收尾被催（升级驱动力即既有闸，零新增执法面）。
- 守卫新增一条：指针目标存在性检查（插件被卸/cache 改址 = `@` 空导入静默——在陈旧闸旁判目标文件在否，不在则显式提醒）。

### 3.2 解锁证据与未验面

- **已实测**：绝对路径 `@` 导入生效——mktemp 对照实验，`CLAUDE.md` 仅一行绝对导入，扩展内嵌二进制 `-p` 问答命中标记串（2026-09-24，`/tmp/imp-test-*`）。
- **必测三件结果（2026-09-24，全部 PASS）**：① **子代理继承 ✅**——`/tmp/imp-sub-*`：双标记串实验（子代理 prompt 无标记、任务无路径），ctxprobe 答 PASS；`/tmp/imp-sub2-*` 升级验真：子代理**逐字回传**导入行原文，`--output-format json` 实查 `num_turns: 2`（Task 调用确证非主控自答）；② **`/context` 呈现 ✅**——`/context` 经 `-p` 可跑（N-4 验证债随带清了首格），导入内容计入「Memory files」类呈现，无报错无失踪；③ **双导入指针形态 ✅**——`/tmp/imp-final-*`：戳+绝对 @宪法+相对 @CLAUDE.project.md 三行文件，两标记串同场各归其位。残余观察项一条：交互会话实录（与 `-p` 同宿主二进制同上下文构造路径，风险低）——随首个真实工程迁移会话确认。

### 3.3 丙（双文件拷贝）— 回退形态

`CLAUDE.md`=纯模板拷贝（升级字节替换）+ 尾部 `@CLAUDE.project.md`（相对导入，`app/CLAUDE.md:333` @NAVIGATION 同款在产机制）。无插件消费方与必测不过时的落点。乙（单文件围栏）弃用——评审 #5：§七中部必填段被「区外直替」静默覆盖。

### 3.4 已枚举并否决的替代（防再问重查）

| 方案 | 否决依据 |
|---|---|
| B' 围栏包住 §七 | = 丙劣化版，围栏位置闸是多余执法面 |
| D2 稳定路径（免 2 行重写） | 宿主无此面（`installed/` 实查仅 superpowers 旧布局；cache 全版本化）；自造 symlink+hook 维护 = 加机器件省一步，净亏 |
| E hook 注入宪法 | 子代理不达 + 宪法非可评审文件 |
| F git subtree | 本质仍合并 + 消费仓耦合规则仓 |
| G 构建期组装 | 平添构建步 |
| 指针免版本路径 | 无稳定面（同 D2） |
| 宿主原生插件级常驻上下文 | Claude Code 无此面，记长期观察，非本批可裁 |

## 4 裁决台账

| # | 事项 | 结果 | 时点 |
|---|---|---|---|
| D-1 | 甲立批实施 | ✅ 带 base 自存 | 09-24 |
| D-2 | 合并默认开 | ✅ `--new-only` 留退路（评审 #10 无异议） | 09-24 |
| D-3 | 终态 | ✅ **D**（先裁丙，新实测证据后再裁 D；丙为回退） | 09-24 |
| D-4 | 无戳老项目（project/saas-cashier） | ✅ 维持拒装人工合并（install.sh:86-87 现状） | 09-23 |
| D-5 | base 来源 | ✅ 项目内自存快照（并入 D-1） | 09-24 |
| — | 甲实施状态 | 代码已落工作区（install.sh 三向合并 + test-self 四态夹具，PASS=61/FAIL=0）；真树负控两工程沙盒副本首轮 8 文件全 MERGE 零冲突零 .new；base_for 无自身戳文件退项目戳（cache 过渡面）；init.md `.new` 叙事已改写；待 flutter-c1 让位后随版本 bump 提交（占 1.0.43） | 09-24 |
| — | 待办 | 存量两工程迁移 D 的批次归属（前置门已开：三必测全 PASS） | 开放 |

## 5 诚实边界

① D 必测三件已全部 PASS（2026-09-24，§3.2）——`-p` 形态实测，交互会话实录与 Windows 路径形态为残余观察项，随首个真实迁移会话/首个 Windows 消费方确认；② 甲落地前 `.new` 全人工流程仍是现状；③ 迁移 D 需一次性过甲的合并机器拆出现有拷贝，迁移批次错开会留「半拷贝半指针」混合态——迁移必须整工程原子切；④ 本稿未动任何生产脚本（提案层）；⑤ 评审 #2 的教训（评审者也会报假事实）已入复盘——所有「实测」声明须附原始输出或可复跑命令。
