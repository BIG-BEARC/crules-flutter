# crules-flutter 吸收方案：订单模块折算治理复盘（2026-09-02）

> 状态：讨论稿（待评审）· 日期：2026-09-05 · 来源复盘：`/Users/chuxiong/Downloads/ai-code/复盘/2026-09-02/复盘-订单模块折算治理与真机溢出修复.md`（saas-cashier `feature/saas-cashier-smart-pos-order-detail`，提交 601253555 / 533b3b026）· 面向对象：crules-flutter 维护者
>
> **2026-09-08 落点修订（基线 0.5.2）**：本文撰写时基线为 0.4.0，现远端已演进至 **0.5.2**（skill 瘦身拆分等）。核心结论不变，落点与版本口径更新：① C1/C2 的「SKILL.md L604-624 Overflow-Safe 节」已不存在，落点改为 **`skills/flutter-rules/references/layout.md`**（+3 bullet）；② C3 落点改为 **`skills/flutter-rules/references/platform-pitfalls.md`**「三方依赖」分节（现 4 卡 → 5 卡，仍在 ≤10 配额内）；③ 版本口径 0.4.1 → **0.5.3（patch）**；④ R1-R4 checklist 落点不变（checklist.md 未受 0.5.x 影响）。
>
> **2026-09-08 增补 C4 + 并批标注**：⑤ 新增 **C4 系统字体回退坑卡**（来源：saas-cashier git 实证三笔 + flutter#154166/#145069 查证——Android OEM 字重裁剪 / Windows 缺中文字体，落 `references/platform-pitfalls.md`「Flutter SDK」分节，5→6 卡，仍在配额内）；⑥ 本方案与 [方案-2026-09-08-适配三选一与字体策略](方案-2026-09-08-适配三选一与字体策略.md) **并批 0.5.3**——同场评审、同批实施、同一笔 CHANGELOG：改动文件重叠（checklist / 双模板 / skill references），拆批 = 同文件二次开刀；两文档独立成稿仅保来源语义（复盘吸收 vs 模板演进）。字体策略的模板正文与 checklist 条目归该方案，C4 坑卡归本方案，互不双写。

---

## 1 目标与范围

**要解决什么**：复盘含 A–H 八类问题，判定哪些升格进 crules-flutter（skill 平台坑库 / checklist / 其他），哪些留在消费工程，哪些不吸收。本文只做判定与预览（= distill 闸的「落盘原文」），**不动任何规则文件**——评审裁决后再实施。

**不做什么**：不动 saas_pos 工程侧文档（项目坑由该工程自行落）；不吸收与母版既有纪律重复的条目；不启动 custom_lint 机械化（二期）。

## 2 核心决策（逐条判定）

### 2.1 框架级 4 坑 → skill「平台坑库」（C1/C2 走闸·需查证；C3/C4 查证已前置完成）

| # | 坑 | 归属 | 预置门槛 | 查证需求 |
|---|---|---|---|---|
| C1 | **Row 内 ellipsis 不生效**：非弹性约束下 Text 取固有宽参与 Row 布局，须先包 `Flexible`/`Expanded` | Flutter SDK | ①实证踩过（本轮多处加固踩着这条） | Flutter 布局约束官方文档（flutter.dev 强/弱约束章节）锚点 |
| C2 | **无断词点长串不自动换行**：UAX#14 下纯数字/字母串（单号/URL/UUID）整串无断词点，`softWrap` 照样横向溢出；解法逐字符 `Wrap` | Flutter SDK | ①实证（订单号换行需求） | UAX#14 说明 + Flutter issue（搜 `softWrap long number`）——并入 Overflow-Safe 节时出处以行内链接/脚注形式附 |
| C3 | **Riverpod defunct 崩溃三板斧**：①dispose 首行落存活闸门 + try-catch ②`postFrameCallback` 内 `if (!mounted) return`（finalizeTree 先于 postFrameCallbacks）③在途异步写入统一被闸门拦截 | 三方依赖（riverpod） | ①实证（delivery_order_notifier.dart:189 断言崩溃，本包预设 A 即 Riverpod） | riverpod repo issue（搜 `defunct markNeedsBuild dispose`） |
| C4 | **系统字体回退不可信**：Android OEM ROM（ColorOS 等）裁剪 Roboto/中文字重（3.22.x 起仅两档，flutter#154166）；Windows 缺可用中文字体——未打包字体依赖系统回退的跨端 App 必踩。规避四要素：字体打包 / 统一注入点（禁内联 TextStyle）/ fontFamilyFallback / 特殊链路文字转图 | Flutter SDK（引擎回退机制）× OS 平台 | ①实证三笔（saas-cashier `bbf976e42` Android 全字重打包 / `0508ac9b5` Windows 普惠体 / `656558508` 内联绕过复发） | flutter#154166 / #145069（已查证 2026-09-08，链接待入卡复核） |

**与既有 3 卡的关系**：C3 入 skill 坑节「三方依赖」分节，首批 3 卡 → 4 卡（≤10 配额内）。iOS 26.x 卡的教训同样适用：查不到出处的字段如实标，不杜撰。

> **C1 落点修正（2026-09-05 复核后）**：skill 已有 "Building Flexible and Overflow-Safe Layouts" 节（SKILL.md L604-624）覆盖 Flexible/Expanded/Wrap 正向用法——按归位测试（同一事实不双处维护），C1 **并入该节加 bullet** 而非新开坑卡；C2（无断词点长串逐字符 Wrap）同为布局溢出主题，随 C1 一并并入该节。修订后：**坑卡仅新增 C3 一张**（skill 坑节 3→4 卡），C1/C2 以「Overflow-Safe 节 +3 bullet」形态落（①Row 内 Text ellipsis 须先包 Flexible/Expanded；②无断词点长串（单号/URL/UUID）softWrap 不生效，逐字符 Wrap 或手动零宽断点；③附本轮实证案例指针）。

### 2.2 checklist 4 处半句级增补（走闸·规范性修订预览）

| # | 落点 | 预览原文（追加内容） |
|---|---|---|
| R1 | 条目 9 资损红线末尾 | `；金额展示铁律——主金额固有宽完整显示不可截断（ellipsis 只作用于划线单价等辅助信息）` |
| R2 | 条目 4 边界场景清单末尾 | `/ 窄屏极值（分栏布局按最小窗口逻辑宽过一遍）` |
| R3 | 条目 8 Git 规范末尾 | `；iOS 生成文件（Generated.xcconfig / flutter_export_environment.sh）按机器路径假变更，pathspec 排除后再提交` |
| R4 | 落点待裁决：checklist Flutter 专项 or skill | `折算/像素复刻自查：一个控件查两遍（尺寸折了吗？字号跟了吗？）；像素级对齐对齐的是信息与视觉规格，不是布局机制（滚动区内外、撑底方式按目标屏取舍）` |

**差集标注**：R1→并入 9 ｜ R2→并入 4 ｜ R3→并入 8 ｜ R4→新增专项条（或并入 checklist Flutter 专项节）。

### 2.3 不吸收项（记理由）

| 项 | 理由 |
|---|---|
| A 折算溢出 7 处 / B 漏折两类 / F 窄列 285.8 / 窄屏自适应三件套 | **项目特有折算工作流**——住 saas_pos 工程自己的复盘与坑卡；若第二项目复现再提名进 skill（「相关性住项目」） |
| H 前两条（cwd 重置 / 凭记忆拼 old_string） | crules 母版 v65/v77 已有对应纪律（跨仓绝对路径、先读后改），双份维护违反归位测试 |
| 预防建议 1（折算对照脚本）/ 2（真机验证前移）/ 4（窄列极值） | 1 属工程工具不属规则包；2 已被 R2 + 既有「静默失败验到现象层」覆盖大半；4 并入 R2 |
| 预防建议 3（提交前自查） | 部分并入 R3；「主仓 git show --stat 确认子模块引用」属消费工程惯例不进包 |

## 3 现状

- skill 平台坑库现为 **4 卡（0.5.1，`skills/flutter-rules/references/platform-pitfalls.md`**——0.5.0 拆分后落位），各归属分节 + `###` 卡标题。
- checklist 为通用 10 条编号 0–9 + Flutter 专项（0.4.0 差集标注格式）。
- 本轮将是 **0.4.0 沉淀闸的首次真实运转**——按蜜月期建议走全闸档。

## 4 目标方案（实施步骤，裁决后执行）

1. **查证**（timebox 30min）：C1–C3 各 1–2 个官方出处（布局文档 / UAX#14 / riverpod issue），查不到的字段如实标「未查证」——坑卡字段凑不齐自动不过闸。
2. **坑卡落位（按头注 2026-09-08 修订后的落点）**：C1/C2 = `references/layout.md` +3 bullet；C3 = `references/platform-pitfalls.md`「三方依赖」分节 +1 卡；C4 = 同文件「Flutter SDK」分节 +1 卡（5→6 卡）。字段全（归属/触发场景/症状/根因/规避/区间/状态=未修复或已修复于 vX/最后核验 2026-09-08/出处=复盘链接+官方链接）。区间写法：C1/C2 = Flutter 全版本（布局语义/UAX#14 属长期行为）；C3 = riverpod 涉及版本按查证；C4 = Android 自 3.22.x（#154166）+ 桌面长期。
3. **checklist 4 处局部 Edit**（按 §2.2 预览原文，差集标注同步）。
4. **CHANGELOG 0.5.3**（patch：内容增补无机制变化）+ 双 json bump。
5. 验证：`grep '最后核验' references/platform-pitfalls.md` ≥7；checklist `资损红线`/`极值` grep；test-self 14/0。
6. 版本口径：**0.5.3（patch）**——纯内容增补，无新机制/命令。

## 5 影响面

| 对象 | 影响 |
|---|---|
| 消费工程（skill/checklist 均随 plugin 分发） | 新坑卡被动获得；checklist 4 处增补使审查口径略收紧（+4 半句，常驻可忽略） |
| saas_pos 工程 | 无（其项目坑自行落，不受本包变更影响） |
| token | skill 按需加载非常驻，+3 卡约 30 行仅触发时加载 |

## 6 测试与验收

| 判据 | 级别 |
|---|---|
| 三卡字段齐全、出处链接可达（抽查） | 静态 |
| checklist grep：`窄屏极值`=1、`金额展示铁律`=1、`pathspec`=1、差集标注含 4 新映射 | 静态 |
| test-self PASS=14 FAIL=0 | 构建级 |
| 双 json 0.5.3 + CHANGELOG 顶部 0.5.3 | 静态 |

## 7 风险与回退

- 查证不足 → 对应卡不预置（结构性过滤器，宁缺毋滥）；R4 落点分歧 → 评审裁决。
- 回退：文件级 git 回退（skill / checklist / CHANGELOG 各自独立）。

## 8 待确认事项

| # | 问题 | 阻塞 |
|---|---|---|
| Q1 | R4 落点：checklist Flutter 专项节 vs skill（「参照物优先」附近）？ | 是（定落点才出终稿） |
| Q2 | C3 区间按查证填；若 riverpod issue 查不到版本区间，卡是否仍预置（实证门槛已满足）？ | 否（默认：字段如实标，仍预置） |
| Q3 | 本轮是否顺手记入 fork-coverage §六（0.4.1 边界：内容吸收无机制）？ | 否（默认记一行） |
