# 复盘 · 2026-09-24 · MQTT 域知识沉淀（网络通信域独立成文件）

> 状态：已发版收口（commits 308de4e/0699358，tag v1.0.46 已推 origin，cache 快照已生效）· 触发：需求方 2026-09-24 指令「mqtt 相关知识也需要整理出来」· 素材源：saas-cashier（纯只读，master-fix2108 线；批1-3 期间反复以「归网络域」暂缓的素材域独立成批）
> 落盘面：`references/network-comms.md`（新建，47 行 15 卡四节）、`SKILL.md` 索引行、`agents/backend.md` 网络通信节、`iot-devices.md` 三处指针改指新文件。

## 结果一句话

MQTT 域 15 卡（A-O）全过收编，落点按需求方裁**独立成网络通信域文件**（MQTT 非外设，不塞 iot-devices）；三例现行缺陷（TV 跨店串台 / favorpay 寄宿 atMostOnce / 未知 code 静默丢）全落标「现行缺陷，接入前自查」；broker 安全面落机制层全脱敏。references/ 8→9 文件。

## 三轮机械检验账（两路采证报告 vs 我方复跑）

| 面 | 引擎/连接路 | 业务主题/案卷路 |
|---|---|---|
| 哈希 | 并集正则抽取 **73 笔去重**：cat-file **零幻影**；merge-base 在线性 **ON 70 / NOT-ON 3**，NOT-ON 恰为两报告声称的三笔支线锚（2d11e59ef/d0f003d5c 仅 tv 支线、173e731c8 仅网扫支线）——两路归属声明一字不差 | 同左（并集核算） |
| 覆盖复算 | 16 项计数**全等**：mqtt 133/订阅 19/推送 45/重连 34/掉线 0/断线 3/丢失 12/漏打 4/串店台单 0/重复播报 1/重复打印 3/ls-tree 19/files 16/listen 5/S-clear 2/putMsg 仅定义 1；发布日志逐词计数两文件全等 | 同左；案卷↔commit 映射表逐行核对 |
| diff 逐字 | 12365c95a 仅 +1 行、6492ba793 四行注释化、a547410d5 QoS 升级行、665f39131 6000→7000、4768f70c6 指数退避引入（生效态） | 693904bbb await 两处、9d8827094 去重行、04b82bcbc key 行、cb1c6fe95 队列引入行+提交体 |
| 现值回读 | :69-72 四行注释/:79 只增不清/:246-254 print 直订/`late entity` :113/五行 QoS 双轨/print 退订注释 :199/config 13 处 static const（只验在位不回显值）/mInterval 演化链五跳补全（20000→6000→7000→指数→20000，a6a580895 注释退避） | onInit 恒空后缀 :64 在位、两 permanent put :52/:54、printer_manager.dart 队列 :54/:556-561、takeaway 九码表+status :243、JWT_KO :32-33、SP 开关 defValue :317 |

## 检验纠偏（落盘时生效，4 条）

1. **printIdQueue 现行落点**：报告指 `printer_service.dart` 在采证线**已不存在**（文件拆分），实为 printer_manager.dart :54/:556-561——「旧路径当现行路径」新亚类（文件改名/拆分后未重定位），机制无损。
2. **外卖播报码表少报**：报告引四码（美团/饿了么×新单/接单），现值实为**九码**（+抖音/京东×2+deliveryOrder）——与批3 已落白名单漂移卡互证；落卡按现值口径。
3. **mInterval 演化链两报告各见一半**：引擎路称「指数退避存在但被注释」、业务路称「4768f70c6 加指数退避」——实测引入时生效、后被 a6a580895 注释改回固定 20000，合并才完整；落卡写全五跳链。
4. **print 失订窗口时间线失准**：引擎路「2024-08~2025-06 print 既不退订也不重订」不成立——该窗口 print 走退订+回调链；「双不」仅 ed8d8ec5c→12365c95a 的 2 天窗口；落卡按正确时间线（并采证当场销案候选池⑨：665f39131 系 MQTT 域重连间隔笔）。

## 裁决记录（2026-09-24，AskUserQuestion 四问全按推荐）

1. **15 卡总裁决 = 全过**（含 4 纠偏执行）；候选池 10 项不落盘、只登记本文档。
2. **落点 = 新建网络域文件**（MQTT 非外设、iot-devices 定位是外设通信）——`references/network-comms.md` + 指针面三处同步。
3. **三例现行缺陷 = 全落+标「现行缺陷，接入前自查」**（与批3 先例一致）。
4. **broker 安全面 = 落机制层脱敏**（凭据/实例名/域名全不进仓）。

## 候选池登记（不落盘，10 项处置口径）

| # | 项 | 处置 |
|---|---|---|
| 1 | data[0] 批量帧丢尾 | 已在卡J 作观察项提及（无实证事故） |
| 2 | late entity 误记 | 升格为卡I（现行缺陷） |
| 3 | _onSubscribeFail 无退避重订 | 卡A 断点③提及，不单列 |
| 4 | 订阅成功清单只增不清 | 卡L 残余项提及 |
| 5 | 断线 toast 无节流 | 纯观察，不落 |
| 6 | isLoginStatue 丢帧窗口 | 卡I 同类第三丢弃路径提及 |
| 7 | 前后台无分支 | 卡M 如实登记（grep 零命中） |
| 8 | 初始化日志明文 password | 卡N 坑③ |
| 9 | 665f39131 域归属未验 | **复验当场销案**：MQTT 域（6000→7000），入卡B 锚 |
| 10 | cleanSession 未显式设置 | 码面无断言，不落（库默认不猜） |

## 脱敏执行

- 泛称/抹除：商户名（卡帕尼亚烘焙屋新兴店→「某烘焙商户门店」、湘聚园）、门店号、订单号、cashierId 值、broker 实例名（mqtt-cn-*）、域名（eshop-mqtt-*）、AccessKey/Secret（含截断形）、clientId GID 字面量、topic 环境前缀字面量、内部链接（yapi）、TAPD 工单号、人名邮箱、公司名、内网 IP、flowId。
- 平台名（美团/饿了么/抖音/京东）作业务码前缀按 I10 保留检索键；机型词不入本批。
- V5 终验：四落盘文件 23 词 grep **全零命中**。

## 版本批（1.0.46）

- bump 三件套：plugin.json / marketplace.json 1.0.45→1.0.46 + CHANGELOG 1.0.46 条目 + README 横幅。
- 缺口闸初跑红→当场学闸语义：**当前 plugin.json 版本系闸自身豁免**（「在建版本被扫即红是时序假缺口非登记缺口」，test-self.sh 缺口闸 `heads.discard(当前版本)` 注释自证）——1.0.46 不登记（批3 曾登记 1.0.43 系因它是他会话的 **past** 未发版头，情形不同）；初跑曾加 1.0.46 行判幽灵，已撤。
- **多会话协调**：本批启动时会话面已推进——他会话完成 1.0.44 收口批后连发 b142fa4/89fcba7/3ad9659/f0052a9，**v1.0.43/44/45 tag 均已在集**（批3 复盘遗留的发版提醒 v1.0.43/44 两枚就此销账），缺口表回到 11 个历史不回补项；本批 bump 在 1.0.45 之上顺延，双 json 闸对账通过。
- **发版提醒**：commit 后发版须补 tag **v1.0.46**（tag 必达闸兜底；在建期间缺口闸豁免、发版后即销）。

## 验收终态（T 面 / V 面）

- **T2**：占位全文件 0；**T6**：落盘文件锚再抽取 **43/43** cat-file 零 BAD。
- **test-self**：61/61 PASS（含坑库症状速查对账闸——iot-devices 指针改写未触面、双 json 相等闸、版本缺口闸、欠账龄闸）。
- **V2**：新文件无裸【待项目沉淀】——15 卡全填实，「无实证事故」「无修复锚」「码面无断言」处如实标注。
- **V5**：脱敏全词全零（见脱敏执行）。

## 错误类清点（累计）

七类预埋清单后：幻影哈希 0、数值漂移 0、覆盖口径全等。新瑕疵 3 处均非七类旧脸：**「旧路径当现行路径」（文件拆分/改名后未重定位）为新亚类**，连同「枚举少报」「时间线表述失准」登记——下批采证提示词预埋清单相应加宽（现值回读一律以 ls-tree 实定位为准，不信报告路径）。
