# 复盘 · 2026-09-24 · 外设域三拆（printer / scale / scanner 独立成文件）

> 状态：已落盘+验收完成（并入 1.0.46 版本批，待 commit 授权）· 触发：需求方 2026-09-24 两句裁决「打印机/电子秤/扫码枪是不是拆分出来比较合适？」+「客显可以并到电子称当中」（MQTT 批收尾时的追加设计问询）· 性质：纯结构重排，零采证、零内容新增。

## 结果一句话

iot-devices.md（拆前 220 行 / 40.7KB，references/ 最大文件）按设备域三拆：**printer.md** 66 行 23 卡六节、**scale.md** 66 行 23 卡三节（客显并秤）、**scanner.md** 26 行 7 卡两节；本体枢纽化为 93 行（铁律 / 文件地图 / 前置门 / 钱箱 / 副屏 / 语音播报 / KDS）。references/ 9→12 文件；74 卡逐字迁移零丢失（行集比对 + 卡数守恒双证实）；指针面五处同步。

## 裁决记录（2026-09-24）

1. **方案 = 三域全拆**（AskUserQuestion，需求方选「三域全拆（你的原案）」而非推荐的只拆打印）。
2. **客显并秤**（拆分执行中需求方追加消息「客显可以并到电子称当中」）——内聚依据成立：客显与秤系平行双实现（serial_util.dart / electronic_scale_serial.dart 互不 import）、互斥同口、共用串口公共底座（config dispose 坑两文件同日同修）。

## 数据面（拆前实测）

- iot-devices.md 220 行 / 40.7KB，references/ 最大文件（次大 platform-pitfalls.md 199 行 / 34KB，其余全部 <80 行）——已超 SKILL.md 自记的当年 39KB 单文件拆分阈值；触发即全量加载（调秤的任务也载入语音播报 mp3 白名单）。
- 域占比：打印 ~60 行 / 秤 ~40 行 / 扫码 ~20 行。
- **检索错位**：文件按「通信方式」分节，任务按「设备」来——修一个网口打印问题要翻蓝牙 / USB / 网口 / 打印专项 / 内置 / 标签六节。拆分即按设备收编，一跳命中。
- 爆炸半径：症状速查对账闸只扫 platform-pitfalls.md（test-self.sh:424 实证），拆分不触；需同步的指针面共五处。

## 拆分映射

| 目标文件 | 收编（自 iot-devices.md 原节） | 卡数 |
|---|---|---|
| printer.md | 蓝牙 5 + USB 打印 4 + 网口 3 + 打印专项 7 + 内置 3 + 标签 1 | 23 |
| scale.md | 串口底座 5 + 秤 11 + 客显 7（自「副屏 / 数字客显」节拆出并秤） | 23 |
| scanner.md | USB HID 4 + 扫码专项 3（节名改「盲扫与业务面」） | 7 |
| iot-devices.md（枢纽） | 通用铁律 8 条 + 文件地图 + Android 14 前置门 + 钱箱 6 + 副屏 7 + 语音播报 8 + KDS 指针 | 21 |

**卡面零内容变更承诺的机械证据**：旧文件（HEAD 版）非空行多重集 vs 新四文件并集，未收录 26 行**逐一归类**＝11 节标题升降级/改名 + 4 文件头改写 + 3 MQTT 批已改写行（vs HEAD 的自然差异，非拆分损耗）+ 8 按裁决改写的指针/导语句；**卡体行零丢失**。卡数守恒：旧 74 = 新 23+23+7+21。

## 改写行清单（8 处，全部是节内互链指针/节导，非卡体）

1. 串口节尾注：「客显卡见『副屏 / 数字客显』节」→「见本文件『数字客显』节」（并秤后同文件）。
2. 秤节导：「秤卡集中于本节与串口 / USB 节」→「集中于本文件『秤』节与『串口』节」。
3. Android 14 括注尾：「本节设备卡」→「printer.md / scale.md 的设备卡」（自 USB 节上收枢纽文件地图节）。
4. 内置 AIDL 卡：钱箱节/副屏节互链 → 显式指 `iot-devices.md`。
5. 钱箱 AIDL 失效卡：「见『内置打印机』节」→「见 `printer.md`『内置打印机』节」。
6. 副屏节导：删客显半句，改「已并入 `references/scale.md`」。
7. 副屏第二引擎卡尾：「与秤 mainLooper 卡同族」→「（`references/scale.md`）」。
8. scanner 节名「扫码专项」→「盲扫与业务面」（HID 卡自 USB 节并入后，节面覆盖两源）。

## 指针面同步（五处）

| 文件 | 改动 |
|---|---|
| SKILL.md | 外设行 1→4 行（打印 / 秤客显 / 扫码 / 枢纽各一行，场景键按设备走） |
| agents/backend.md | 设备通信节改四文件分指 |
| platform-pitfalls.md Android 14 卡出处行 | 「iot-devices.md USB 节 / 副屏节」→「printer.md（USB 打印卡）/ scale.md（秤卡）/ iot-devices.md 副屏节」 |
| network-comms.md 节尾 | 网口打印→printer.md、串口设备→scale.md、语音播报→iot-devices.md |
| build-release.md 混淆节 | 秤锚落点 iot-devices.md→scale.md |

## 版本批口径（并入 1.0.46，不另立 1.0.47）

缺口闸语义：闸只豁免**当前 plugin.json 版本**（test-self.sh `heads.discard(当前版本)`）。若拆分 bump 1.0.47，则 1.0.46 头成「有头、无 tag、又非在建版本」的**真缺口**，闸必红——登记表只收历史不回补项，不能收在建版本。故拆分并入 1.0.46 版本批（多批收口一个版本批，D9 先例），CHANGELOG 1.0.46 条目扩写承载；version 三件套不动（仍 1.0.46）。

## 验收终态

- **卡数守恒**：74 = 23+23+7+21（顶格 `**` 机械点验，旧新相等）。
- **行集比对**：未收录 26 行全数归类为改写/标题行，卡体行零丢失（见上）。
- **行数**：printer 66 / scale 66 / scanner 26 / iot-devices 93，合计 251（旧 220；+31 系四份文件头与文件地图节的结构性增量）。
- **test-self**：全套复跑见下方实数行。
- **经验**：预写数两处口算失准（秤 11 误记 12、行数 93 误记 92）——落 CHANGELOG 前以 wc/正则实数回填，「自报不可靠、验证须外置」对本仓自身写作同样适用。

## test-self 复跑实数

**PASS=61 FAIL=0**（2026-09-24 三拆落盘后全套）：死链闸（新三文件互链零死链）、文件锚定散文指针闸、消费面黑话禁令闸、版本缺口闸（1.0.46 在建豁免成立，11 历史项全在册）、双 json 相等闸全过；坑库症状速查对账 17 卡不受影响（闸扫 platform-pitfalls.md，事前判断与实测一致）。
