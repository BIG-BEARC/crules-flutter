# 复盘 2026-09-23 · 消费工程 1.0.40 升级 + 三向合并试跑

> 范围：仓库拉取 1.0.26→1.0.40、插件 cache 更新、三消费工程处置。方案沉淀另见 docs/方案-2026-09-23-升级合并体验优化.md（本复盘即其证据来源）。

## 事实线（均本会话实跑）

- 仓库 ff `929719e..333abfc`（1.0.40），test-self 52/0 随版在册
- 插件：VSCode 扩展内嵌二进制 `claude plugin update crules-flutter@crules-flutter-market` 1.0.25→1.0.40；verify-cache 双特征串（plugin_version/version_key）命中 cache 实物三文件
- 三工程：①saas-suite/…/saas-cashier v1.0.25→v1.0.40 已合并落盘；②saas-cashier-light v1.0.6→v1.0.40 已合并落盘；③/Users/chuxiong/project/saas-cashier 无戳——护栏拒装、未动盘（D-4 待裁决）
- Stop 闸复验：两已合工程「宪法版本落后」提醒消失（light 余 1 条真队列项，属工程自身活）；/tmp 非定根静默

## 合并手法（本次人工替代品的实然形态）

`git merge-file --diff3`，base=cache 旧版模板、ours=现状、theirs=.new。两工程各仅 1 冲突块、同形（项目尾部内容撞戳位/gen 块）；8 伴生用户零改动（对 base diff=0 实测）直替。落盘前锚点核对 + 备份 /tmp/merge*/、/tmp/mergelight*/.bak-premerge。

## 踩到与自纠

1. **「看不到差异」真因**：.new 未跟踪→git diff 空面 + 现状文件未被改动→源代码管理面板无红。教训=升级工具须自带差异呈现（方案甲 §0.1）。
2. **grep -cE 转义坑**：`\|` 在 ERE 不认交替，致「§十三 grep=0」虚惊，换写法复核后内容在位——红线「汇总时逐条回看原始输出」当场咬住一次自报。
3. **`rm -rf` 预演误触 deny-list**：合并试跑中命令含 `rm -rf` 字样被拦，改 mktemp 目录形态合规通过——闸按设计工作。
4. **saas-suite 工程 `.claude/memory/` 疑云为误报**：初查 cwd 漂移落错目录报「整个不存在」，复核查实际在位（14 项）。虚惊记一笔：路径结论须 pwd 钉死后再报。

## 遗留

- 工程③并法方向、方案甲 D-1~D-4、light 工程队列 1 条待补索引。
