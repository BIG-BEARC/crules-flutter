# 复盘 · 2026-09-24 · 硬件外设批2 沉淀（电子秤 / 串口 / 副屏）

> 状态：已落地（待 commit 授权）· 关联方案：[方案-2026-09-23-硬件外设知识沉淀.md](方案-2026-09-23-硬件外设知识沉淀.md)（v6）· 素材源：saas-cashier（纯只读，master-fix2108 线）
> 落盘面：`iot-devices.md`（串口节填实 + 秤节新建 + 副屏节填实 + 通用铁律补例 + USB 节/商米 T1 卡指针）、`platform-pitfalls.md`（Android 14 新卡 + 速查表同步）、`build-release.md`（vendored SDK keep 一条）。

## 结果一句话

批2 两域 29 卡全过（含 3 处检验纠偏 + 现值修正）：电子秤 18 + 副屏 11 收编；Android 14 族（副屏卡1/2/3 + 秤 S13）经需求方**开禁**主落 platform-pitfalls.md 一卡合并 + iot-devices 指针——方案 §1「批2 不动 platform-pitfalls」被需求方本轮裁决覆盖。iot-devices.md 127 → 184 行。

## 三轮机械检验账（采证报告 vs 我方复跑）

| 面 | 电子秤（18 卡） | 副屏（11 卡） |
|---|---|---|
| 哈希 | 自报 73/73；我方 python 全文抽取 **283 去重**（含无料/候选池），cat-file 逐笔 + `--no-walk` 批解析 **282/283**，唯一失配 `c5607ce6e` 见下纠偏① | 87/87 解析零 BAD（前轮已完成） |
| 标题比对 | 46+30 关键锚 subject 与卡文主张逐笔相符（三连 Revert 谱系、cherry-pick 八笔同题、「防止在界面上改重量」等） | 前轮已完成 |
| diff 逐字 | S1 改前后两行、S5 M→S 门、S14 minify true→false、S13 DETACHED 补注册、S1 提交体「静默少称或卡 0」 | 00c486752 双错、a70e66e1b/d0abdfd8e 孪生实锤（见纠偏） |
| 现值回读 | provider 五方法、Timer 三处、isTopProtocol 三头、isDaHuaError 签名、3s 窗+200 注释态、latest-wins、flags :141/:333、断开顺序体、so 改名+四 ABI、pubspec 回滚态、DeviceHelper 全点、keep 规则、日志 10s/3s、ex 'S' 判稳 | 前轮 ~15 项全在位 |
| 覆盖复算 | 报告口径（无 --all）逐项复跑 **128/99/27/13/8 + union 262 + 路径 163 = 全等** | --all 口径 70/4/16/85 全等（前轮） |

## 检验纠偏（落盘时生效）

1. **秤报告 B 簇两处幻影哈希**：`5f5e038d53`/`5c5607ce6e` 实为 10 位形（正则面外），真身 `5f5e038d5` / `c5607ce6d`（末位转录错）。两笔均在**无料清单**，不影响任何卡锚——落盘面 65 锚全部独立复验通过（见机械验证）。
2. **秤卡 S9 数值漂移**：报告「validationLatch.await(3000ms)」抄自 CHANGELOG_COMPATIBLE.md 示例段；现行 CompatibleScaleReader.java `VALIDATION_TIMEOUT_MS = 1500`。落盘卡文改写机制不写死数值（DeviceHelper 侧 2s=20×100ms 轮询为另一处、属实）。
3. **秤卡 S8 sleep 值**：报告「断开后 sleep(100)/重建前 sleep(200)」；现行两处**均 200ms**（:305/:334）。落盘泛写「各 sleep 等 fd 释放」，注值随版本演化。
4. **副屏卡10 孪生**（前轮预告，本轮实锤）：a70e66e1b 在本分支仅含 esc_pos_utils 改动（-11 行），Kotlin 可空化改动在 **d0abdfd8e**（3 文件 +41/−24）——落盘引用指 d0abdfd8e，批1「跨 lineage 旧拷贝」教训活例。
5. **副屏卡9 交叉引用失准**（前轮预告）：platform-pitfalls Win7 节不含 8a912208b 副屏内容，「主内容已入 Win7 节」不成立——卡9 按独立副屏卡落，商户名「茶元素」泛化为「实测案例」。

## 裁决记录（2026-09-24，AskUserQuestion 四问）

1. **29 卡总裁决 = 全过**（含纠偏与现值修正）。
2. **Android 14 族 = 开禁**——platform-pitfalls.md 主卡（OS 级一卡合并：广播三参门 + Presentation 窗口类型门 + filter/action 一致性子坑），iot-devices USB 节与副屏节留指针；本批动 platform-pitfalls.md 获准（方案 §1 不动面被需求方裁决覆盖）。速查表同步（test-self 对账闸在位）。
3. **S14/S8-4 落点 = 秤节主卡 + build-release 指针**——R8 keep vendored SDK 一条入 build-release 混淆节；EventChannel 主线程/fdsan 两条通用铁律并入秤节崩溃族卡（不另开 platform 卡——需求方未选双落平台，按「秤节主卡」选项收口）。
4. **审查过判三例 = 不落分发面**——仅作采证/评审纪律，留本复盘。

## 脱敏执行

- 「茶元素」→「实测案例」；内网测试 IP（10.199.190.129）→ 泛称「曾硬编码内网测试 IP 残留」；「bugly」→「线上崩溃上报」；「嘉联/jlpay/HCD/甄灿兵/xgd」零入盘。
- 品牌机型名按 I10 保留为检索键：大华 / 顶尖 / 奥家（Aclas）/ HCD 插件路径名 / ef60 / 商米。
- 落盘面 grep 商户词/环境词/IP 全零命中（终验见下）。

## 跨批耦合登记

- **批3 来取料**（秤报告 §四已备）：89bc52b2b（关开关清 SP 释放 com 口，diff 细节已采）；serial_util.dart 为客显/秤共用工具（S10 两文件同修）；D 簇客显主料 425e58d2b / 7a036f353 / e4e995b73 / ac49c312c；候选池 3c23b11d4（结账后屏蔽开串口）；47 笔客显 commit 面 + TAPD bug_id（客显域）留批3。
- **蓝牙节结论**：秤零素材（检索确认），占位说明撤、无需 BLE 秤卡。
- **S3 尾注**：v2.5.13「顶尖称无法连接」与 37fe85197/a93271fc5 无法一一锚定——候选池不升卡。

## 机械验证（T 面）

- **T2**：占位归零——「【待」全文件 0 命中（批1 余 1 → 0）；文件 127 → 184 行。
- **T4**：`git diff --check` 绿；SKILL.md / backend.md 分节列举同步（+秤 / 副屏）。
- **T6**：落盘新增面程序化抽取 **65 锚**，`--no-walk` 批解析 **65/65 OK 零 BAD**（含硬非空守卫）。
- **test-self**：56/56 PASS，含**坑库症状速查对账**（新卡 + 表行逐字一致闸）。
- 脱敏终验：三落盘文件 grep（bugly/rum/云店/茶元素/jlpay/xgd/人名/IP/mylekha/嘉联）——纠偏 1 处后全零命中。
- 版本三件套：本批不触（D9：批3 收口为一个版本批）。

## 错误类清点（供批3 采证提示词引用，累计）

批1 五类 + 本轮新增：**哈希位数抄录错（9↔10 位末位漂移）**——无料清单同样要过 cat-file，不能只核卡锚；**文档示例值当现行值**（S9 3000ms 抄自 CHANGELOG 示例段，现行 1500ms）——数值断言必须回读代码本体，档面引语只作机制出处。
